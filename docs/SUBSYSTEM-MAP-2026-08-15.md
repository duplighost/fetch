# FETCH subsystem map - 2026-08-15

Generated by six read-only mappers against branch claude/aug15-notes @ ca8ec27.
Exact file:line anchors for the change-order build (see HANDOFF.md top section
and CHANGE-ORDER-2026-08-15.md). Line numbers drift as you edit - trust names
over numbers once the build is underway.



---

# MAP: OPENING

All source read. Compiling the report.

REPORT — FETCH opening (C:/Users/Alex/Projects/fetch-aug15), files: src/house.js, src/main.js, src/director.js (+world.js/player.js/skull.js where the opening's machinery actually lives)

=== 1) WAKE / SPAWN / BED / COVERS ===

Spawn: director.js:8-13 `ACT_SPAWNS.bedroom = { x: 9.6, z: 3.3, yaw: 2.37, y: 3.6 }` (comment claims "x sits just clear of the bed frame at 9.76"). Applied at main.js:247-250: `player.pos.set(spawn.x, 3.6, spawn.z)` (y hardcoded 3.6 = HOUSE_TABLES.levels.first.floor, house.js:14), `player.yaw = spawn.yaw`, `player._sync(0)`, then `checkpoint('bedroom')` (main.js:251). yaw 2.37 → forward (−sin, −cos) = (−0.699, +0.716), aimed at window center WIN_C (7, 5.4, 6) 3.75 m away. Eye height EYE=1.62 (player.js:6) → camera y 5.22.

Bed: `K.bed(10.55, F, 3.35, 0, 1.58, 2.18, true)` house.js:950; kit bed() house.js:248-267. Frame box 1.58×0.2×2.18 at y+0.3 → spans x 9.76..11.34, z 2.26..4.44; mattress (w−0.08) top at y 4.175; corner posts at x 9.735/11.365, z 2.235/4.465 (tall 1.62 posts + headboard on the SOUTH side, z≈2.26 — the head of the bed faces the rug/board/bell corner); two pillow spheres at z≈2.67. `bare=true` skips the merged top cover (house.js:252-255).

TRAP — "you start out inside the bed": spawn x 9.6 is only 0.135 m west of the bed post face (x 9.735) and 0.16 m from the frame slab (x 9.76); player capsule RADIUS 0.34 (player.js:7) spans x 9.26..9.94, z-position 3.3 is inside the bed's z range 2.26..4.44 → the player's body overlaps the bed frame/mattress footprint on frame one, and nothing ever pushes them out because NO furniture has colliders (see §2). The loose covers "frozen wave" mesh (0.9×0.06×0.85 at world ≈ (9.77, 4.16, 3.77)) reaches x 9.32..10.22 — right beside/under the camera. Walking backward/right passes straight through the bed.

Covers meshes: house.js:951-986. `root` group at (10.55, F, 3.35). `on` group = 3 boxes: main drape 1.05×0.07×1.5 D.linen at local (−0.28, 0.655, 0.45) rz 0.1; frozen wave 0.9×0.06×0.85 at (−0.78, 0.56, 0.42) rz 0.5; tail 0.62×0.05×0.55 D.linenDirty at (−1.02, 0.26, 0.5) rz 1.05. Hidden `imprint` 0.5×0.012×1.85 color 0x544f45 at (0.1, 0.591, −0.02) ("sleeper's compression, slightly too long"). Hidden `heap` = 3 boxes at local x −1.18..−1.28 (floor level, west of bed). `P.covers = { root, on, imprint, heap, pos:(10.1, F+0.7, 3.7) }` house.js:985.

Covers animation (this is the "odd" one — it is NOT a wake cinematic; it only plays when the player presses E on the bed): searchable 'covers' house.js:1809-1820. hitBox (10.15, F+0.64, 3.75) size 1.6×0.35×1.6 (spans x 9.35..10.95, z 2.95..4.55 — the spawn point is inside its XZ footprint; camera at y 5.22 is above the box top 4.415 so raycast still works). dur 0.85 s, sound clothDrag. drive (house.js:1812-1818): rigid-body transform of the whole `on` group — `position=(−1.1e, −0.42e, +0.18e)`, `rotation.z=0.85e` (48.7° roll about the ROOT origin at bed-center FLOOR level), `scale=(1−0.15e, 1−0.38e, 1−0.2e)`. Numerically at e=1 the main drape center lands ≈ (8.77, F−0.20, 3.98): the group sweeps through the bed's west rail and ends ~20 cm BELOW the floor plane; `imprint.visible=true` pops binary at p≥0.55; finish() (1819) hard-swaps `on.visible=false; heap.visible=true`. So the read is: three rigid boxes rotate/sink through bed+floor, then teleport-swap into a pre-baked heap. No easing of the swap, no cloth deformation.

Wake sequence: there is none beyond the title card. Title overlay click → startGame() main.js:833-843 (started=true, audio.init, fadeIn(2.4), director.start, pointer lock). No forced look, no covers animation, no get-up. Boot ordering: main.js:265-271 — `if (act==='bedroom' && !flags.has('skullArrived')) skull.bootAbsent()` (skull.js:793-829, census-safe light parking; mode='gone'), which must run AFTER pinLightCensus (main.js:262). Room boot light: house.js:1185-1197 — bedroomLamp candle {x 9.4, y F+1.35, z 5.5, intensity 2.6, r 4} pushed to world.candles, plus moonPatch PointLight 0x9fb6cc intensity 7 dist 3.6 at (7, 5.1, 5.55); ticker at house.js:2051-2052 damps lamp 2.6→1.7 when arrival state==='done' and moon 7→0 once glass.broken.

=== 2) BEDROOM FURNITURE + COLLIDERS ===

How furnishings get colliders in general: THEY DON'T. `world.box()` (world.js:76-88) emits merged geometry only; `addCollider` (world.js:66-74) is a separate manual call, and inside furnish() (house.js:828-1460) it is never called. Player collision is capsule-vs-`world.colliders` only (player.js:210-244). The only colliders that exist in the bedroom: room walls (world.js buildHouse), window sill AABB (world.js:631-632), the base window-aperture AABB with `skullPass:true` (world.js:669-671 — blocks the player forever, lets the skull through, never collapses), the bedroomDoor collider (world.js:1212-1213, collapses when open), the act-added pane collider (house.js:1649 `world.addCollider(6.15, 4.6, 5.8, 7.85, 6.2, 6.2)`, collapses at shatter), and the exterior tree-bough collider (house.js:1561 `addCollider(2.2, 6.3, 8.6, 8.8, 11.5, 12.0)`). audit-furnishings.mjs is static placement-vs-openings analysis (parses K.* calls out of the source; catalog tools/audit-furnishings.mjs:89-117) — it audits nothing about colliders. TRAP: world.js:122-150 buildGroundContact derives under-furniture contact decals FROM colliders ("furniture added later is grounded without anyone remembering to") — since furniture has no colliders, bedroom furniture gets no grounding decal either.

Inventory (everything walk-through by the player):
- Rug, notched L: merged via world.box, two bodies (9.25, F+0.012, 3.675, 4.3×2.15) + (8.5, F+0.012, 2.175, 2.8×0.85), bound edges + fringe house.js:849-862. Loose corner flap group at (10.65, F+0.042, 2.6), flapBody 1.5×0.018×0.85 at local z −0.425 (house.js:863-874). `P.rug.pos=(10.65, F+0.05, 2.2)`.
- Claw gouges (F1c): group at (10.13, F, 2.38) ry 2.92, four 0.5×0.012×0.045 dark boxes, house.js:877-889 — added VISIBLE at boot, physically concealed by the flap lying on top.
- Loose floorboard + cavity + BELL (F1a): boardPivot at (11.03, F+0.024, 2.16) rz 0.003, board 0.85×0.016×0.11 at local x −0.425 (hinge at EAST end, free end west to x≈10.18), house.js:896-904. Cavity 0.9×0.008×0.16 black at (10.62, F+0.008, 2.16) hidden (906-909). Bell group at (10.42, F+0.065, 2.16): brass cylinder body lying mouth-west, torus lip, iron handle, rotted strap torus, brass tag, two rope stubs; `bellG.visible=false` (911-940). `P.board.pos=(10.62, F+0.05, 2.16)`, `P.bell.pos=(10.42, F+0.1, 2.16)`.
- Bed (above) + loose covers group.
- Wardrobe `K.wardrobe(5.15, F, 4.92, π, 1.48, 2.22, true)` house.js:989 merged carcass; loose hinged doors on hinge groups at (5.15∓0.658, F+1.13, 4.595) ry π (992-1013); interior rail at (5.15, F+1.92, 4.85) + 4 hangers (one child-sized coat at i===2) (1016-1036).
- Dresser `K.dresser(9.35, F, 5.52, π, 1.28, 0.94, skipDrawer=1)` house.js:1041; loose middle drawer group at (9.35, F+0.515, 5.235) with linens + coiled leather LEASH (F1b) (1043-1080). `P.dresser.baseZ=5.235`.
- Nightstand `K.nightstand(8.68, F, 3.98, −0.08, 0)` house.js:1084; loose small drawer group with candle stub + 3 matches (1085-1120).
- `K.oilLamp(9.35, dresser.top+0.01, 5.45)`, `K.book(8.68, F+0.68, 3.98, 0.18, 2)` house.js:1123-1124.
- Curtains `K.curtains(7, F+0.02, 5.84, π, 1.9, 2.2)` house.js:1128; panels = PlaneGeometry children at ±1.01 from center (1129).
- Framed art on nail pivot at (4.17, F+1.99, 2.35), falls-painting material, hangs rx 0.045 crooked; behind it F2 unfaded wallpaper patch 1.06×1.24 color 0x454c40 at x 4.145 (house.js:1134-1152).
- Door slide-latch hardware on bedroomDoor panel (plate/bolt/knob/staples at local (0.48, 0.45, −0.06..−0.08)) + invisible hit 0.36×0.3×0.1 (house.js:1159-1181).
- bedroomLamp candle + moonPatch (above). Outside: tree trunk/canopy/branch/string/key (house.js:1512-1534), locket (1563-1576).

=== 3) RUG → FLOORBOARD → BELL CHAIN ===

Searchable system: house.js:1730-1766. `hitBox()` (1735-1741) makes invisible MeshBasicMaterial boxes (raycastable — three's Raycaster never checks visibility). `addSearchable(id, hitObjs, {dur, enabled, sound, drive, finish})` (1742-1766): one-shot (`rec.done`), disables its interacts on press, `arrival.searchedCount++`, plays `sound()` IMMEDIATELY at press, then a ticker drives `drive(easeIO(p), p, dt)` over `dur` seconds, then `finish()`. Records live in `game.bedroomSearch`. E + crosshair-grow is the only affordance (crosshair data-target at main.js:1424-1426).

S4 RUG (house.js:1825-1839): registered on the flap body mesh itself. dur 0.7, clothDrag rate 1.35. drive: `P.rug.flap.rotation.x = −2.7 * e`. TRAP (matches the note "rug rotation flips INTO the bed"): the flap body sits at local z −0.425; rotation about +X by −2.7 rad maps it to y′ = −0.18, z′ = +0.384 → final resting pose is 14 cm BELOW the floor plane at world z≈2.98, inside the bed footprint (bed south edge z 2.26). Mid-animation (θ=−π/2 at e≈0.58) the flap points straight DOWN through the floor (0.42 m under). The intended "folds back north on top of the rug" pose is rotation.x = +2.7 (y′ = +0.18 above the pivot). Sign is inverted. finish(): enables boardInters, plus a 0.3 s "settle proud" ticker `P.board.pivot.rotation.z = 0.003 + 0.017·t/0.3` + knock (1830-1837) — see next trap.

S5 FLOORBOARD (house.js:1841-1857): `addSearchable('floorboard', P.board.pivot, { dur 0.8, enabled:false, ...})` — registerInteract on a group auto-builds an inflated invisible hitbox (world.js:324-332: Box3 + 0.15, min 0.4 each axis). sound() fires at press: creak + `cavity.visible=true` + `bell.group.visible=true` (1845-1849). drive: `P.board.pivot.rotation.z = 0.02 + 0.59 * e` (1850). TRAP (matches "floorboard rotates DOWN through the floor"): board extends to local x −0.425 from its east-end pivot; rotation.z POSITIVE maps the free end to y′ = −0.425·sin(0.61) ≈ −0.243 → the west end swings 24 cm DOWN through the floor instead of hinging up and propping. Hinge-up requires negative rotation.z. Same sign family: the "settles proud" ticker (0.003→0.02) actually sinks the free end ~8.5 mm below the floor, and even the boot pose 0.003 puts it 1.3 mm under — never proud. finish() (1851-1856): knock verb 0.5, `arrival.bellFound=true`, state 'searching'→'bellFound', `bellInter.enabled=true`.

THE BELL interact (house.js:1938-1959): registered on hitBox (10.42, F+0.16, 2.16) 0.52×0.4×0.5, `bellInter.enabled=false` at boot (1959). Handler: `if (bellCool > 0) return;` — silent. `canSummon = (state==='searching'||'bellFound') && !flags.has('skullArrived') && skull.mode==='gone'` (1940-1941). If canSummon: bellCool=3, rockT=1.4 (rock anim ticker house.js:2046-2049), `bellRung=true`, state='called', bellRing {gain 1.25, rate 0.72, verb 0.95, dark} + `audio.duck(0.22, 2.6)` (1942-1951). Else if state!=='done': bellCool=1.2, rockT=0.35, dead strapped thud (1952-1957). Else (state 'done'): silent no-op.

E-dead-but-visible intervals (the note "interval where the bell is visible but E does nothing"): (a) the bell becomes VISIBLE at floorboard press-start (sound callback, 1848) but bellInter is only enabled at floorboard finish() 0.8 s later (1855) — during the whole board animation the bell is visible, the crosshair will not grow on it, and E does nothing; (b) `bellCool` windows (3 s after a summon ring, 1.2 s after a dead clunk) return at line 1939 with ZERO feedback — no sound, no motion.

Mercy cue: house.js:2039-2041 `const BELL_MERCY_TINK = false;` — "Discoverability mercy, built DARK... One line to enable." The ticker branch (2063-2069) would, at state 'searching' && !bellFound && searchedCount>=5, play a muffled under-floor glassTink every 6-10 s (mercyT seeded 8 at 1679). It is DISABLED. There is no other escalation/hint for finding the rug/board.

State machine `game.bedroomArrival` (house.js:1680-1720): { state:'searching', t, searchedCount, bellFound, bellRung }, plus `completeInstant()` (teleport/test contract: force post-arrival room, idempotent, silent; stands down if `director._respawnPreArrival`, house.js:1686-1706) and `resetForRespawn()` (1710-1719: state back to bellFound|searching, bellRung=false, bell re-enabled iff found, searches persist, broken window stays broken). Director side: director.js:1058-1059 snapshots `preArrival = !flags.has('skullArrived')` before respawn teleport; 1112-1119 re-asserts absence (delete flag, skull.bootAbsent(), bedroomArrival.resetForRespawn()).

=== 4) INCOMING-SKULL SEQUENCE ===

One ticker runs the room (house.js:2044-2140). Timeline after the ring: state 'called' — 2.0 s authored silence (2073-2082) → 'incoming': u = min(1, t/9.5), e = u^1.35 (accelerating crescendo), position = pathAt(e) along AP_PATH = [(4,5,46), (2.5,4.4,20), (4.5,3.9,10), (7,5.4,6.55)] with AP_FRAC [0, .638, .888, 1] (1965-1980); boneApproachStart at t=0, skullMoanStart joins at t≥7 with speed-based intensity (2084-2098); path authored to keep ≥1.2 m from the treeKey at (7.2, 5.68, 8.2) (comment 1961-1964); at u≥1 → shatter() (2097).

shatter() (1993-2014): if glass not broken — pane removed, glassInter disabled, `glassCollider.max.y = min.y` ("the added collider ONLY — never the skullPass base", 1999), rim shards visible, fireBurst() launches the 14 pooled shards into the room (vel z −2.2..−5.6, gravity 6.5/s, retired at 1.2 s; 1982-1991 + 2054-2062), glassShatter; if already broken (mid-arrival restart re-summon) just a frame knock (2003-2007). Then impact('break'), `skull.pos.copy(P_OUT=(7,5.4,6.55))`, `skull.arriveRestore()` (skull.js:831-852 — root+lights restored, mode STAYS 'gone' so no _collide/_checkTargets can run), `_inV=(0,−0.5,−1)·26`, state='inbound'.

'inbound' (2100-2125): first 0.25 s lerp P_OUT→P_IN=(7, 5.05, 4.55) straight through the aperture; then segment B homes on the LIVE camera cradle: `cradle = camPos + lookDir·1.35; cradle.y −= 0.4`, desired velocity 26 m/s toward it, blended `_inV.lerp(des, 1−e^(−9dt))` (2111-2118). `s.root.lookAt(cam)` each frame. Catch: `if (s.pos.distanceTo(cam) < 1.5 || arrival.t > 1.2) doCatch()` (2123). TRAPS (match the notes): (a) the cradle follows camera PITCH — looking at the floor puts the cradle at y≈3.5 (below the 3.6 floor), looking up puts it near the 6.4 ceiling, and the flight has NO collision in mode 'gone', so the skull visibly chases into floor/ceiling; (b) the t>1.2 timeout fires doCatch() wherever the skull is — doCatch (2016-2037) calls holdNow() and clamps the visual catch offset to 0.44 m along the incoming direction with a 0.24 s settle lerp, so a timeout catch teleport-snaps it out of the floor into the hands.

doCatch → 'flicker': `beginIntroFlicker()` (skull.js:854-866; 3.6 s human-head↔bone strobe driven in _updateHeld skull.js:963-979); hands rise (`_handPose.g` damp, 2130); at `!introFlicker && t > 0.4`: `flag('skullArrived')`, `checkpoint('bedroom')`, bellInter disabled, state='done' (2131-2136). NOTE: skullArrived lands ~3.6 s AFTER the physical catch — a death during the flicker respawns as pre-arrival (director.js:1058).

=== 5) E / INTERACT SELECTION ===

main.js:1272-1305. E edge (KeyE, main.js:635) → `_interact()`: `_crosshairTarget()` else `snapBuffer = FEEL_PROFILE.snapBuffer (0.12)`. `_crosshairTarget()`: Raycaster from screen center, `far = 2.9`, `intersectObjects(this.world.interactables, false)` — the ray is cast against the INTERACTABLES LIST ONLY. Walls, furniture, the bed, the merged shell — none are in that list, so nothing occludes: E selects an enabled interactable through any nearer solid geometry within 2.9 m (the exact trap in the notes — e.g. the rug-flap/board/bell hitboxes sit 10 cm south of the bed headboard and can be E'd through the bed; searchable hitboxes are inflated boxes, world.js:324-332, min 0.4 per axis). The hit loop (1300-1303) returns the first ENABLED inter and skips disabled ones (so a disabled nearer hitbox does not mask, but also does not block). Same-frame crosshair affordance: render() sets `ch.dataset.target = _crosshairTarget() ? '1' : ''` (main.js:1424-1425) — the growth is also un-occluded, so the crosshair grows through walls too.

=== 6) WINDOW GLINT / STRONG GLASS ===

The table row for THE window is `['first', 9, 9, 'S', { open:true, w:1.7, id:'bedroomWindow' }]` house.js:86. Because open:true, _spawnWindow (world.js:646-706) builds: frame boxes, NO built-in glass, the skullPass aperture collider (670), and the aim glint — a LineLoop rectangle 0xbecbd0 base opacity 0.055 hugging the interior frame face (678-694), registered in `windowOpenings` with {center (7,5.4,6), normal (0,0,1), width 1.7, height 1.6, glint, hot:0, aim:0} (695-704). The act then ADDS the pre-arrival strong pane (house.js:1641-1649): MeshStandardMaterial 0x0a1016 opacity 0.51 + its own non-skullPass collider — only that pane/collider is ever removed (comment 1634-1640).

Aim affordance: main.js:1307-1342 `_updateWindowAimAffordance(rdt)` — called EVERY RENDER FRAME from render() (main.js:1383), ungated by act, skull mode, or glass state. It ray-plane intersects the camera forward ray with each opening (dist 0.15..24), `aimed` when the hit lands within ±47% of width/height; `opening.aim` eases toward it (rate 13/s); opacity = 0.045 + aim·(0.58+pulse) + hot·(0.36+pulse), pulse = sin(t·9)·0.055, scale 1+0.025·aim. `hot` decays 0.7/s and is only ever pumped by the ground-floor relay windows (house.js:3599-3600) — never the bedroom. TRAP: pre-arrival, while the glass is authored-unbreakable, looking at the window still lights the full throw-aiming glint (0.045→~0.63 opacity, pulsing) — an aiming affordance for a verb the player does not have yet and for glass that cannot break. Glass breaks ONLY via shatter() (bell → 2.0 s + 9.5 s incoming) or `completeInstant()` (debug/test teleport, house.js:1686-1706). E on the strong pane: glassInter (house.js:1725-1728) — dull `glassKnock` + shake(0.05), deliberately rattle-free ("rattle grammar means openable-but-locked; this is a wall that happens to be glass", 1722-1724).

=== 7) DEAD INPUTS PRE-SKULL ===

Input wiring main.js:592-636; verb gate `verbsLive = !dead && !ended && !_basementExit` (1178). With skull.mode 'gone' (bootAbsent, and STILL 'gone' through the whole arrival flight until doCatch):
- LMB press (throwPressed): requires `mode==='held' && !introFlicker` (main.js:1182) → nothing, no feedback.
- LMB release (throwReleased): requires 'outbound' (1186) → nothing.
- RMB tap (<260 ms, main.js:603-608): mode 'gone' → `director.onVoidCall()` (main.js:1188) — director.js:423-451: sets `_voidCool=5`; only the cave act does anything. In the bedroom: total silence.
- RMB hold (callHeld): consumed only in mode 'poised' (skull.js:896) → nothing.
- E into empty air: `_interact()` falls through to `snapBuffer=0.12`; the buffer loop calls `skull.call()` (main.js:1191-1199) which returns false for 'gone' (skull.js:738-750; beginReturn also refuses 'gone' at 753) → buffer decays silently. Zero feedback.
- E on things: searchables/glass/bell as in §3/§6; the bedroomDoor panel is a registered interact (world.js:1216) — locked rattle.
- Space jumps, WASD/Shift move normally (player.js). skull.update case 'gone' is a bare break (skull.js:884).
After the catch, LMB stays gated another 3.6 s by introFlicker (main.js:1182), by design ("the palms must close before you can throw it", 1179-1181).

Cross-cutting traps recap: rug flap folds through the floor into the bed footprint (house.js:1828, sign); floorboard hinges down through the floor (house.js:1850, sign; also the "settle proud" ticker 1832-1837 sinks it); covers = rigid group rotate/translate through bed+floor with binary imprint/heap pops (1812-1819); spawn capsule starts overlapping the bed silhouette with no collider anywhere in the furniture set (director.js:13 + player.js:7 + absence of addCollider in furnish()); bell visible-but-inert for the 0.8 s board animation and silent during bellCool (1846-1855, 1939); mercy tink authored but compiled out (2041); E selection and crosshair growth ignore occlusion (main.js:1294-1305); window glint advertises aiming at unbreakable glass every frame (main.js:1383, 1307-1342); inbound skull follows camera pitch with no collision and a 1.2 s timeout catch (house.js:2111-2123); skullArrived checkpoint lands ~3.6 s after the catch, so a flicker-window death replays the pre-arrival room (house.js:2131-2136, director.js:1058).


---

# MAP: COLLISION

All four files read. Report follows.

================================================================
FETCH collision + ceilings map — C:/Users/Alex/Projects/fetch-aug15
(src/player.js 302 L, src/world.js 1297 L, src/skull.js 1336 L, src/house.js 6793 L)
================================================================

1) PLAYER CAPSULE-VS-AABB (src/player.js)
- Constants: EYE=1.62 (player.js:6), RADIUS=0.34 (:7), STEP_UP=0.5 (:8), HEAD=1.75 (:9), WALK=2.7/RUN=4.7 (:10-11), GRAV=14 (:12), JUMP_V=5.2 (:13).
- Colliders live in `world.colliders` (world.js:45), a flat array of `{min:{x,y,z}, max:{x,y,z}, ...flags}` built by `World.addCollider(x0,y0,z0,x1,y1,z1, flags)` (world.js:66-74). No spatial index; the player iterates the entire array twice per frame (x pass then z pass).
- Algorithm — `Player._moveAxis(dx,dz)` (player.js:210-244), called for x then z (player.js:145-146). Per collider:
  - `c.max.y <= feet + STEP_UP` → skipped as "walkable step" (:215). TRAP: groundHeightAt never reads collider tops, so a sub-0.5 m box is not stepped ONTO — it is fully phased through.
  - `c.min.y >= feet + HEAD` → skipped as overhead (:216).
  - Else circle-vs-rect in XZ only: clamp pos to box, push out radially if d < RADIUS (:218-228); if the center is inside the box, push out the shallowest of the four XZ faces (:229-239). It is a vertical-axis cylinder test, not a true capsule; Y is never resolved here.
- Vertical (player.js:162-187): entirely `world.groundHeightAt(x,z,curY)` (world.js:340-370) — best of (a) ramp lerp heights where `h <= curY+0.55`, (b) room floorY (skipping floorHoles) where `floorY <= curY+0.55`, (c) terrain only where no non-basement room overlaps (world.js:365-368). Falling snaps to gh; descent glides at 8 m/s (:183); ascent lerps at dt*14 (:185).
- CEILINGS ARE NOT HANDLED AT ALL for the player. There is no upward collision test anywhere in player.js: `fallV`/`pos.y` rise unchecked; ceiling slabs are visual-only (see §3), and even a real AABB overhead never clamps Y (colliders only push in XZ). Concrete case: basement floor -3.0, ceil -0.55 (house.js:12) — a full jump (apex = 5.2²/2·14 ≈ 0.97 m) puts the camera at -3+0.97+1.62 = -0.41, inside the basement ceiling slab (visual slab spans ≈ -0.55..-0.33, world.js:486 `ceil+0.11`, th 0.22). The swing (PULL=30 vs GRAV 14, player.js:131-134) can lift the player clean through any ceiling; groundHeightAt then resolves the storey above and they land upstairs.
- Stair-rail shed: `_railShed` (player.js:253-286) — grounded on a ramp's `treadColliders`, a blocking AABB that is either tagged `stairPart:'edge'` with matching `stairId` (:268-269) or hugs the flight's side boundary within 0.15 (:270-272) re-aims the blocked perpendicular push down-flight; sets `_railTouch`, creak rate-limited (player.js:150-160).
- Player ignores `skullPass`/`door` flags entirely; open doors stop blocking only because `Door.setOpen` collapses `collider.max.y = min.y` (world.js:1220-1224), which then fails the STEP_UP test.

2) SKULL FLIGHT COLLISION (src/skull.js)
- `_collide(ctx)` (skull.js:1237-1279), radius r=0.1 (:1239). Runs only in modes outbound/poised/returning (update switch :878-885); mode 'gone' never collides (:884, guaranteed by comment at :836-839).
- Ground: same `groundHeightAt` (:1243); below gh+r → snap up, vy *= -0.62, xz *= 0.96 (:1244-1250).
- AABBs (:1254-1278): skip if `c.skullPass` (:1255 — "the open window lets it through"); point-in-expanded-box overlap test at the POST-integration position only (no sweep); push out the shallowest of 6 faces, reflect that axis, then `vel.setLength(min(bounceCap 44, max(speed*bounceGain 1.05, 6)))` (:1270-1275, FEEL_PROFILE :34-35); `break` after the FIRST collider (:1277) — one AABB resolved per frame.
- TUNNELING TRAP: no swept test vs AABBs. Detection window through a WALL_T=0.26 wall (world.js:36) is 0.26+2r = 0.46 m; at 60 fps any speed > ~27.6 m/s steps over it. Return speeds run 26 floor..50 cap (+14 snap bonus) (skull.js:38-41), so returning skulls routinely pass through interior walls. Fetch targets do NOT tunnel — `_checkTargets` is a swept segment prevPos→pos vs spheres (skull.js:1305-1323).
- WHY IT GOES THROUGH THE HOUSE CEILING: room ceilings are NEVER colliders. `World.buildHouse` emits ceiling slabs via `_slabWithHoles(cmat, …, ceil+0.11, 0.22, holesC)` (world.js:484-487) which calls `this.box(...)` only (world.js:799-802) — merged visual geometry, zero collider. Floors identically (world.js:482). Roof slabs in house.js:750-766 are `world.box` only. So a skull thrown upward passes the ceiling slab, passes the next storey's floor slab, and then `groundHeightAt` (one-sided by the `<= curY+0.55` rule, world.js:356) captures it as GROUND of the storey above — it lands upstairs; thrown up through the first-floor ceiling it exits the roof into the sky. Not "one-sided ceilings," not a skull exemption: the geometry simply has no collision representation. Colliders are symmetric two-sided AABBs; there is no oneWay flag anywhere in the codebase.
- Skull-vs-ceiling where a ceiling IS a real AABB: standard shallowest-face pushout + y-reflect + bounce gain (e.g. the bedroom "front boughs" blocker house.js:1561, y 6.3..11.5, exists purely so over-the-house throws bounce back).
- Ricochet failsafes: `_bounceFx` pinball guard — 3 bounces inside 0.4 s → outbound returns, returning force-triggers the stuck fallback (skull.js:1281-1297).
- Minor trap: collapsed colliders keep their full XZ footprint at zero height (Door.setOpen world.js:1223; bedroom glassCollider house.js:1697/:1999). A skull rolling at floor level (pos.y = gh+0.1) exactly meets `max.y + r` and can y-reflect on the invisible plane of an OPEN door (skull.js:1257,1266-1267). The pump gate and window-relay backplate avoid this by parking at y -20..-19 instead (house.js:6071-6072, :3630-3631) — parking, not collapsing, is the safe retract pattern.

3) HOUSE COMPILATION FROM HOUSE_TABLES (house.js:9-110 → world.js buildHouse :375-545)
- Grid: origin [-12,-14], CS=2 (world.js:10); wx(cx) = -12+2cx, wz(cz) = -14+2cz. Levels: basement floor -3.0/ceil -0.55; ground 0/3.3; first 3.6/6.4 (house.js:11-15).
- Walls: per cell edge, `_emitWall` (world.js:586-644). Solid wall segments get colliders spanning full floor..ceil (world.js:617-620). Window sills get colliders floor..sillY (:629-633). Lintels above doors/windows get NO collider (:627 `solidBase=false`) — the strip above every door (2.25..ceil) and window is skull-permeable and player-irrelevant.
- Windows: `_spawnWindow` collider spans the aperture; `{skullPass:true}` iff `opts.open` (world.js:669-671). Open windows in tables: livingRelayWindow, studyRelayWindow, sculleryCrawlerWindow, bedroomWindow (house.js:79,80,84,86) — always player-blocking, skull-passable.
- Doors: collider mid±w/2, floor..floor+h, ±0.13 thick, flag `{door:this}` (world.js:1212-1213); collapse on open (:1223). `door` flag also excludes them from the AO bake (world.js:221) and ground-contact decals (world.js:136).
- Floors/ceilings: visual slabs only, floor-0.11 / ceil+0.11, th 0.22, rect-subtracted around floorHoles/ceilHoles (world.js:468-488, :784-803). floorHoles: main stair shaft first(6,2)-(7,5); cellar shaft ground(10,8)-(11,9) (house.js:101-104). ceilHoles: stairbay-up, bcorr-up, return-flight headroom basement(8,9)-(9,9) (house.js:105-109). Holes affect visuals + groundHeightAt + nav only — never colliders.
- Ramps (house.js:89-100, compiled world.js:491-504, built :805-913):
  - mainStairs: cells x6-7,z2-5 ground→first ⇒ world x 0..4, z -10..-2, y 0→3.6. NOT openUnder ⇒ built by the legacy wedge branch (world.js:901-913): each step is a `this.box` filling from min(y0,y1)-0.12 up to its tread height — a full visual WEDGE with ZERO colliders (that branch registers nothing; treadColliders stays []). Walkability is groundHeightAt's ramp lerp.
  - cellarStairs: x0:10,x1:11,z0:8,z1:8.25 ⇒ world x 8..12, z 2..4.5, y 0→-2.0, `openUnder:true, edgeGuards:true, guardHeight:0.78, edgeOpenAtEnd:2, mat stone, guardMat metal` (house.js:91-95). openUnder branch (world.js:824-899): 11 thin treads (rise 0.185, treadThickness 0.12) each with a real collider flagged `{stairId,stairPart:'tread',stairStep}` (:840-842), plus continuous side edge-guard colliders `{stairPart:'edge',stairSide,stairStep}` (:844-864) on all but the last `edgeOpenAtEnd=2` steps. Treads are walkable from above via STEP_UP and a headroom stop from below via HEAD.
  - cellarReturn: x0:8,x1:9,z0:9,z1:9 ⇒ world x 4..8, z 4..6, y -3.0(at x=4)→-2.0(at x=8), NOT openUnder ⇒ another zero-collider visual wedge (world.js:901-913).
- THE UNDER-STAIR MASS MISMATCH (current state — the "house.js ~575 region" stack of boxes from the older branch is GONE in this worktree; ~575 is now boiler anatomy):
  a) cellarReturn wedge, house.js's own comment confirms it as walk-through: buildBasementPilot moved the pilot because "(7,B,5.48) sat the whole fixture INSIDE the cellarReturn wedge — backboard, cage and glass sprouted out of solid stone… stair mass begins at x=4" (house.js:2428-2432). The wedge (x 4..8, z 4..6, filled -3.12 up to as high as -2.0) has no colliders; a player on the bcorr floor (-3) walks bodily through the visible stone anywhere the ramp lerp is > curY+0.55 above them (east of x≈6.2 at floor level).
  b) mainStairs wedge: same defect at scale — under the high end (z near -2) the visible mass fills 0→3.6 with nothing solid; a ground-floor player walks straight through it inside the stairbay.
  c) cellar flight skirt (house.js:737-748): one sloped stone panel (BoxGeometry 3.96×0.09×3.3 at (10,-1.19,3.24), rotation.x = atan2(2.0,2.5)) added as a raw scene mesh to close the see-through gaps between the thin slats — explicitly "Visual only; no collider" (:741). Skull and player pass through it; only the tread AABBs above/behind it are solid.
  d) foundation skirt (house.js:717-735): brick band y -0.78..0.02 around the ground-floor perimeter — `world.box` only, no colliders. The perimeter band between basement ceil (-0.55) and ground floor (0) has no wall collider at all (walls build floor..ceil per level).
- Roof: house.js:756-766, `world.box` only — skull exits through it.

4) world.box() / addCollider PATTERNS + THE LAW
- `world.box(mat,x,y,z,w,h,d,ry)` (world.js:76-88) = VISUAL ONLY: pushes segmented BoxGeometry into `_geo` per material; `finishStatic()` (world.js:90-112) merges per material, bakes vertex-AO from the collider-shell grid (`_buildOcclusionGrid` :213-242, `_bakeContactShading` :244-282). world.box never creates a collider; addCollider never creates a visual. Every solid visible thing is a MANUAL PAIR.
- `addCollider` flags in actual use: `skullPass` (skull-permeable, player-solid), `door` (Door back-pointer; excluded from AO + decals), `stairId/stairPart('tread'|'edge')/stairStep/stairSide` (rail-shed), `id`, `secretId`, `routeId`. NO oneWay, NO side/normal flag exists anywhere — one-sidedness is emergent (floors support from above only via groundHeightAt's `<= curY+0.55`; nothing blocks from below except real AABBs).
- THE ENGINE LAW (do not fill under the cellar ramp): world.js:817-823 — "Cellar flights need to remain a real piece of architecture from both sides: you walk on top of them, but the high end hangs above a usable basement corridor. The old descending-step formula filled every tread down to basement floor, producing a fake solid wedge; house.js then hid that mistake behind a second stack of boxes. `openUnder` authors actual thin treads instead… no broad invisible blocker and no place to walk through wood." The bcorr basement spawn corridor (cells 4-11×8-9, world x -4..12 z 2..6, floor -3) runs UNDER the cellarStairs high end; any solid fill there breaks the canonical descent + corridor. house.js:741 restates the corollary for the skirt: visual only, never a collider. Related guard: groundHeightAt's terrain clause (world.js:359-368) exists specifically so terrain y=0 can't pin the cellar ramp ("makes the canonical house → basement descent impossible").
- Retract patterns: collapse (`max.y = min.y`) — doors (world.js:1223), bedroom glass (house.js:1697, 1999 "the added collider ONLY — never the skullPass base"); park at y -20..-19 and restore — pumpBridgeGate (house.js:6071-6075), windowRelay backplate (house.js:3630-3631).

5) FURNITURE COLLIDERS + DECAL GROUNDING
- NO K.* furnishing registers a collider. The furniture kit's `box()` helper routes to `world.box` (house.js:169-182) or a loose Mesh when `dynamic` — visual either way. `furnish(game)` (house.js:828-1460) contains zero addCollider calls: beds, wardrobes, tables, shelves, counters, the boiler, crates, barrels — the player and the skull pass through all of it. Chairs/searchable hitboxes (e.g. house.js:1770, 1779, 1177-1180) are invisible interaction meshes, not colliders.
- The complete list of hand-authored colliders in house.js: :1561 front boughs (skull blocker over the bedroom window, no visual of its own); :1649 bedroom glass (pre-arrival pane, non-skullPass, collapses at :1697/:1999); :3141/:3145 crawlCageFront/Side (skullPass:true — "the broad player colliders deliberately let the skull pass; the rendered gaps are the affordance, not fake collision" :3115-3116); :3586 windowRelayBackplate; :5335/:5337 pumpChannelNorth/South (skullPass, y B-0.1..B+2.4); :5356 pumpBridgeGate (skullPass); :5411 pumpWinchBody (skullPass); :5465 blindArchiveShelf:x ×2 (player+skull solid).
- Decal grounding pass — `World.buildGroundContact(scene)` (world.js:128-195): scans `world.colliders` for furniture-shaped boxes (non-door, 0.12 < height < 2.3, footprint area 0.09..7 m², sx,sz ≤ 4.2, min.y within 0.3 of a room floorY, center inside that room) and lays one InstancedMesh multiply-blend radial decal per hit at floorY+0.012, sized footprint+0.42. TRAP: the header comment ("furniture added later is grounded without anyone remembering to", world.js:126-127) is only true for collider-bearing props; since the whole furnishing kit registers none, K.* furniture gets no contact shadow (and only marginally participates in the AO shell via its world.box geometry — the AO grid is built from COLLIDERS (world.js:218), so collider-less furniture also casts no baked occlusion onto the room).
- Cheapest correct existing pattern for a new solid prop (the blind-archive shelf is the cleanest exemplar, house.js:5463-5466):
    world.box(M.woodDark, x, B + 1.15, 4.25, 0.3, 2.15, 3.1);                   // visual, merged, AO-baked
    world.addCollider(x - 0.17, B, 2.72, x + 0.17, B + 2.25, 5.82, { id: … });  // solid; add skullPass:true if throws must pass
  Keep height under 2.3 and area 0.09..7 with min.y on the room floor and the ground-contact decal comes free. Colliders are axis-aligned only — for a rotated prop, author the AABB of its footprint (nothing in either resolver understands ry; world.box's ry is visual-only, house.js:180).

Cross-file traps worth pinning as regressions
- No ceiling colliders anywhere ⇒ skull escapes any room upward; player escapes via swing or the basement jump-clip. Only explicit AABBs (boughs :1561 pattern) stop vertical exits.
- Non-openUnder stair wedges (mainStairs, cellarReturn) are walk-through visible mass — the exact opposite mismatch of the law world.js:817-823 fixed for cellarStairs.
- Skull tunnels 0.26 m walls above ~27.6 m/s @60fps (no sweep, one collider/frame, skull.js:1254-1277).
- Low boxes (top < feet+0.5) are phased through, never stood on (player.js:215 + groundHeightAt ignorance of collider tops).
- Collapsed zero-height colliders keep full XZ footprint; skulls at floor level can reflect off open doors (prefer the park-at-y-negative-20 retract).


---

# MAP: GRAVEYARD

FETCH graveyard map report — repo C:/Users/Alex/Projects/fetch-aug15, files cited as file:line (line numbers from current working tree).

=== 1) GEOGRAPHY ===

Graveyard surface (src/outside.js buildGraveyard, 122-218)
- Zone/surface: world.addZone('graveyard', -22, 6, 26, 44, -2, 12) and addSurface('dirt', same rect) at outside.js:125-126. Yard = x[-22..26], z[6..44].
- Ground: 48x38 displaced PlaneGeometry centered (2, 0, 25) (outside.js:129-142), height field Math.sin(x*0.23)*Math.sin(z*0.31)*0.22 (matches terrainHeightFn outside.js:105). Stored as game.graveyardGround.
- Perimeter: iron fence, addFence runs at outside.js:164-171 — west x=-20 (z 6.5..42), east x=24 (z 6.5..42), north z=42 split around the gate gap FOREST_GATE.x±1.6 (0.4..3.6), south z=6.5 tucks into the house corners (-20..-11.9 and 11.9..24; comment: "the yard is CLOSED", playtest 3). Each run gets a collider y -1..2.2 (outside.js:161-162). Instanced posts = game.graveyardFencePosts (outside.js:172-181). The fence IS the map edge readable; beyond north fence is the forest treeline (atmosphere pass).
- FOREST_GATE = { x: 2, z: 43 } exported at outside.js:13. Forest zone rect starts at z=44 (outside.js:116), so act flips graveyard→forest via zone scan (director.js:356-363).
- Basement arrival: there is NO physical surface hatch. house.js:3064-3081 registerInteract('hatch') on the basement bilco panel at (-10, B+2.1=-0.9, 4.4) (house.js:2995; B=-3.0 from HOUSE_TABLES.levels.basement.floor house.js:12) → game.exitBasement() (main.js:1017-1036) = fadeOut(1.2) + teleport('graveyard'). Spawn = ACT_SPAWNS.graveyard { x: -8, z: 8, yaw: -2.86 } (director.js:16, "car left, gate/bodies ahead"). hatchKey flow: gotHatchKey (house.js:2886-2890, ash pan), hatchUnlocked (house.js:3052), hatchOpen (house.js:3076).
- Wrecked car: group at (-9, -0.02, 14), rot.y -0.96 (outside.js:2340-2342); AABB collider from Box3 (2344-2346); dying SpotLight head at (-8.2, 0.8, 15.6) targeting (-2, 0.4, 24), 300 intensity flicker ticker (outside.js:191-202, drives game.wreckLens).
- Bodies: 4 dragged bodies at [-5.8,18.3], [0.2,22.4], [5.9,27.1], [-1.6,33.1] (outside.js:2414), all oriented AWAY from FOREST_GATE (2770-2771); drag marks instanced (2426-2442). game.graveBodies / game.graveDragMarks.
- Grave arena ground (combat area): the whole yard; commitment line is player z >= 18 (director.js:645). Wave spawn sites [-17,13],[20,13],[-17,38],[20,37],[-8,34],[13,29],[-2,21],[10,18] (director.js:676-679).
- Hero (destructible) graves, buildDestructibleGraves (outside.js:581-787): sites [-16.2,12.1], [-2.5,12.7], [7.2,13.4], [18.3,16.8], [-10.8,30.1], [18.1,37.2] (583-586). #6 = index 5 at (18.1, 37.2) — 2 hits topple (collider.max.y→0.25, flag `graveToppled:6`, outside.js:694-700). Fetch ids `breakableGrave:1..6` (663). Debris: fixed 36-cap InstancedMesh pool game.graveDebrisPool (611-634).
- Floating rubble key: makeKey (imported from house.js:10), position (18.52, 0.85, 37.5), scale 1.4, bobbing/rotating (outside.js:433-447, 524-530). keyGlow candle descriptor same spot (446).
- Sealed mausoleum (canine shrine) = EAST mausoleum at (15.6, 31.5) — game.sealedMausoleum (outside.js:239, 265). West mausoleum at (-14.6, 34.2) = game.ritualMausoleum (264) — the REQUIRED one with the ossuary floor entrance. Both built at outside.js:239-272; doorway is a 1.0m gap in the -z (south, house-facing) wall, black voidMat "darkness" plane at z-1.735 (261); five colliders per mausoleum leave only that gap (266-270).
- Open-grave pits (skullPass colliders): [-7.2,27.0], [9.2,23.0], [-14.2,18.4], [11.8,36.2] (outside.js:275, 293). The NE pit (11.8, 36.2) is THE MARROW's mouth: game.marrowPit = { pit, collider, x:11.8, z:36.2 } (outside.js:297). NOTE: (70,-10) is NOT the surface mouth — it's the underground district origin (marrow.js:25-26).
- Resonant graves (the "ritual"/three-toll stones): [-15.2,27.2], [14.5,19.4], [7.4,35.0] (outside.js:800), fetch ids `resonantGrave:1..3` (850).
- Main surface gate: buildGraveyardGate (outside.js:921-1027). Two leaves hinged at (0.4,0,42) and (3.6,0,42); header (2, 2.7, 41.96); 3 latch weights at x 2+(i-1)*0.62, y 2.37, z 41.82 (961-968) — each ritual credit drops one weight (gate.setRitualStage 972-975, ticker 1001-1009). gate.collider (0.32,-1,41.82)-(3.68,2.55,42.18) at 970; collapses when gate.t > 0.32 (1015). gate.openGate() only creaks open — it is ONLY called from the ossuary counterweight solve (outside.js:2017) and director restore paths (director.js:199, 1086).
- The ORIGINAL key tree: bedroomAct's tree in house.js:1512-1533 — trunk (5.5, 4.5, 11.5), canopy (5.5, 9.6, 11.5), branch reaching toward window, key hung on a string at (7.2, 5.68, 8.2), fetch id 'treeKey' → skull.grab('bedroomKey') → flag gotBedroomKey (house.js:1543-1552). This tree stands physically inside the graveyard strip (z 8-13.5) just NE of the house. The locket hangs deeper in the same canopy at (5.2, 7.55, 13.85) (house.js:1575), front-bough collider (2.2,6.3,8.6)-(8.8,11.5,12.0) (1561). The skull's window-arrival flight path is authored to clear the treeKey by >=1.2m (house.js:1961-1966, AP_PATH via (4,5,46)→(2.5,4.4,20)→(4.5,3.9,10)→(7,5.4,6.55)).
- Atmosphere dressing (src/atmosphere.js buildGraveyardDress 305-...): instanced headstone families on a 9x10 grid (x -17+col*4.15, z 9.2+row*3.75), with `reserved` exclusion circles at outside landmarks (atmosphere.js:323-327: [-9,14,4.25] car, both mausoleums r3.5, the 4 pits r1.9, the 3 resonant graves r1.9) and a funeralPath spline [-8,8.2]→[2,41.4] kept clear (313-322). Two tall gothic stones frame the gate at (-0.65,40.4) and (4.65,40.4) (349-350). Adding a new landmark REQUIRES a matching `reserved` entry or instanced stones will intersect it.

=== 2) FLOW / FLAGS ===

Resolution (two routes, director.js):
- graveArena state machine _updateGraveyardArena (director.js:637-707): armed on entering graveyard (spawns 1 dormant walker (-14,24) + 2 'standing' gravePressure walkers (8,30),(16,16), director.js:218-232); trigger = player z>=18 → wave object, wakeAll radius 60, skullScream (645-651). Waves 1/2/3 spawn 4/5/6 graveArena walkers with rise anim; inter-wave breath a.t = 1.35 + min(0.45, wave*0.15) (666). After wave 3 cleared → _completeGraveyard('loud') (706).
- Ritual route: onGraveResonance(index, pos) (director.js:709-722) — credits Set, per-credit setCredited(true) on the grave (bowed-fork silhouette, outside.js:893-900), gate.setRitualStage(credits.size) (weight drops), wakeAll r13; 3 credits → _completeGraveyard('ritual') → extra flag 'funeralRite' (736).
- _completeGraveyard(route) (director.js:724-750): sets graveArena.done, graveRitual.done/route, flag graveyardResolved, gate.setRitualStage(3) (gate does NOT open), g.ossuary.unlock(route), checkpoint('graveyard', pose x -14.6, z 31.2 — facing the ritual mausoleum).
- ossuary.unlock(route) (outside.js:1730-1741): sets flags graveyardResolved + ossuaryOpened, hides ritual mausoleum darkness, shows surfacePit + stairThroat, stone-grind. Idempotent (this.unlocked guard).
- Ossuary interior: flag ossuaryEntered on crossing the throat (outside.js:1954); counterweight solve (progress>=1 after 1.7s anchored hold, outside.js:1994, 2011-2027) → flags ossuaryCleared + graveyardCleared, graveyardGate.openGate(), checkpoint; kennel solve → flag ossuaryKennelSolved (outside.js:2081); climb-out → flag ossuaryExited + setAct('forest') + checkpoint('forest') (1794-1799).
- Restore path: director._enterGraveyard resolved branch (director.js:195-215) — re-stages gate, re-unlocks ossuary, and if graveyardCleared forces ossuary.solved=1, progress=1, exitT=1, exitCollider collapsed (207-209: exitT is "the ONE number the far end derives from"). respawn() pre-resolution resets arena/ritual/gate/resonant/destructible/ossuary (director.js:1072-1080); post-resolution re-asserts (1081-1087).
- Mausoleum-secret chain (outside.js:319-579): topple hero grave #6 → key visible (derived per tick from grave.hits>=2, never a flag: outside.js:509-519) → keyTarget 'mausoleumKey' (outbound-only + 0.9s settle delay via keyRevealedAt, 453-468) → flag gotMausoleumKey; hit 'mausoleumLock' at (15.6, 0.94, 29.56) while carrying → flag mausoleumUnlocked, grate sinks 2.45m (grateCollider.max.y tracks grate mesh, 545-548) → IRON CANINE tooth visible on plinth (15.6, 1.14, 32.05) → 'ironCanine' target (outbound, requires mausoleumUnlocked, 490-505) → flag skullSharpened → idempotent apply block (551-577): game.skullPower = 2, sockets ×1.13, inverted-hull rim shells. skullPower effects: enemies.js:1880-1913 — contact threshold 8/power, stunT = spec.stun*power, knockback ×power, flourish longer; marrow statue shove 1.9*power (marrow.js:766). Carried-key visibility law: key.visible = carried || (toppled && !gotten && surfaceVisible) where surfaceVisible excludes ossuary/marrow occupancy (outside.js:516-519).
- MARROW flags (marrow.js): marrow:entered (564), marrow:witnessed (571), marrow:guardianYielded (890), marrow:relicKept (940), marrow:escorted (956).
- Danger-sense relic (the marrow relic heartbeat): marrow.js:662-679 — runs EVERY tick everywhere (before the `if (!state.inMarrow) return` at 681). If state.dangle exists (a 0.45-scale clone of the relic added to game.skull.jaw at relic take, 944-948): nearest enemy distance over game.enemies.list, danger = clamp(1 - near/14, 0, 1), pulse phase advances at 0.9 + danger*5.1, throb = sin²; dangle scale 0.45 + throb*(0.1+danger*0.16), emissiveIntensity = base + throb*(0.4+danger*2.4). Value/motion only, no UI.

=== 3) THE OSSUARY UNDER-YARD ===

Constants (outside.js:1040-1045): OX=-70, OZ=-10, FLOOR=-4.2, HALF_W=3, LENGTH=30, HEIGHT=2.85. Corridor x[-73..-67], z[-10..20]. routeRoot 'required graveyard ossuary', visible=false until occupied (1046-1049), batched via batchStaticGroup at 2156.
- Rooms: ossuaryMain, ossuaryPocketWest (OX-5.9..OX-3, OZ+10.7..13.3), ossuaryPocketEast (OX+3..OX+5.9, OZ+17.7..20.3) (1091-1098); plus ossuaryShaft/ossuaryShaftLanding/ossuaryShaftTop (1384-1391). Re-registers a 'graveyard' audio zone + 'dirt' surface over the offset rect (1099-1104).
- Topology: 3 alternating baffles at z = OZ+7.5, +14.5, +22, blocking left/right alternately with 1.75m mouths (1123-1138). Candle descriptors behind each baffle (1152-1157); bone niches (1162-1207); instanced ribs (1209-1227).
- Entry (surface side): ritual mausoleum (-14.6, 34.2): surfacePit plane at (x, 0.035, z+0.15), sinking surfaceSlab, 5-tread stairThroat z-0.38..+0.7, entryLamp candle (1231-1250). Walk-over trigger (no verb): unlocked && act graveyard && skull HELD && |p.x-x|<0.58 && z in (34.08..35.4) && p.y>-1 → teleport to (OX, FLOOR, OZ+0.85) yaw π facing down-corridor (1935-1956); clears graveArena/gravePressure enemies; checkpoint('graveyard'); spawns the district resident: 'walker' in 'standing' mode at (OX-1.6, OZ+20.4), tether 2.2, mesh.userData.keepInOssuary=true (1962-1975). Walk back pre-solve: p.z < OZ+0.28 && held → teleport to (-14.6, 0.04, 33.0) (1977-1989).
- Counterweight: mechanism at (OX, FLOOR, OZ+26.2) (1256), wall-mounted (WALL_X = HALF_W-0.1, axle/bearing/plinth/corbel 1265-1303). Fetch target 'ossuaryCounterweight' on the wheel r0.86 → skull.anchorAt(anchorPos (OX, FLOOR+1.35, OZ+26.1), maxHold 4.5) (1854-1867). Hold 1.7s fills progress (1994); release decays ×1.4. Solve (2011-2027): flags ossuaryCleared + graveyardCleared, gate.openGate(), resident _layToRest.
- Exit slab: at (OX, FLOOR+1.33, OZ+28.15), collider tagged ossuaryExit (1304-1309); collider tracks the sinking STONE, opens only when slab top < FLOOR+0.05 (2030-2038).
- The two-flight climb (1310-1405): shaft chamber z OZ+28.6..35.4; DECK_Y = FLOOR+5.25 (=1.05), WALL_TOP = DECK_Y+1.65; flight A 11 treads x OX+0.7..+2.85, z OZ+29.55..33.25, rise FLOOR→FLOOR+2.1, ramp id ossuaryFlightA (1343-1354); landing pillar; flight B 6 treads x OX-2.0..+0.7, z OZ+33.9..35.4, FLOOR+2.1→+3.25, ramp ossuaryFlightB (1367-1378); hatch platform pillar x OX-3..-2 top FLOOR+3.25 (1381-1383). Parapet edge colliders tagged stairPart 'edge' (1393-1405). Black shaftVoid plane at WALL_TOP (1336-1340) keeps the sealed-district "zero exterior visible" invariant.
- Deck hatch + chained lid (1407-1508): deck plates at DECK_Y; mouth curbs + bone roots + header (1422-1437); lidPivot at (OX-2.45, DECK_Y+0.07, OZ+35.3) with handle bar (basement bilco language, 1443-1471); hatchChains X + hasp + brass padlock under the mouth (1472-1503); shaftGlow candle INSIDE the mouth (OX-2.45, DECK_Y-0.6, OZ+34.75) (1507). All poses parametric off state.exitT (2029-2064): chains drop smoothstep(0.04,0.34), lid opens smoothstep(0.3,0.98)*1.62 rad (regression asserts >1.6; 1.92 clipped the wall — comment 2044-2046), shaftGlow 1.0→4.4.
- Climb-out verb: invisible exitMouth box (OX-2.45, DECK_Y+0.18, OZ+34.75) 1.0×0.55×1.3, interact id 'ossuaryClimbOut' (1808-1815). Enabled only when inOssuary && solved && exitT>0.98 && p.y>FLOOR+3.05 && p.x<OX-2.0 && p.z>OZ+33.9 && skull held (2139-2141). Verb only ARMS (_pendingClimbOut); the district ticker executes doClimbOut AFTER cullers, before seal restore (1769-1806, 2142-2151 — ordering contract documented; crossedFarExit → forest.syncBackDistrictCulling(true)).
- Forest-side arrival mouth (1869-1918): 'ossuary arrival hatch' group at (FOREST_GATE.x, 0.08, FOREST_GATE.z+0.3) = (2, 0.08, 43.3); curbs, black mouthVoid, below-ground shaft walls, arrivalPivot lid opening -smoothstep(0.05,0.95,exitT)*1.85; skullPass collider ±0.68 (1913-1915); registered as lookback root (1918). Climb-out teleports player to (2, 0.12, 44.35) yaw π (facing +z, into forest), setAct('forest'), forest.recentre, checkpoint('forest'), arrivalKick settle (1776-1805, 2058-2064).
- Sealed-district visibility (1817-1852): while inOssuary, every top-level scene child is hidden except camera, skull.root, routeRoot, impact fx, any isLight, world.lightRoot (see pin note below), and userData.keepInOssuary; saved/restored exactly via ossuarySaved Map.

=== 4) THE MARROW DISTRICT (src/marrow.js) ===

Constants: MX=70, MZ=-10, FLOOR=-5.0, HALF_W=4, LENGTH=26, HEIGHT=3.4 (marrow.js:25-30) — "mirror of the ossuary's offset, east". Hall x[66..74], z[-10..16]; marrowRoot visible=false (276-279). Audio zone registered as 'basement' + 'stone' surface (327-330). Room 'marrowHall' level 'marrow' (322-325).
- Entry: E-verb on the pit plane — pitInter = world.registerInteract(game.marrowPit.pit, 'marrowDescend', descend) (599-602), enabled when graveyardResolved && !inMarrow && act graveyard (651). doDescend guards (551-556): act graveyard, flag graveyardResolved, skull HELD, and hypot(p.x-11.8, p.z-36.2) <= 2.6 (HARDCODED surface coords). Teleport to (70, -5, -8.5) yaw π (557-563); flag marrow:entered; checkpoint('graveyard'); enterPalette. First entry: witnessed script — Presence erupts at (MX+0.8, MZ+9) after 1.1s, folds after 2.6s (569-577).
- Exit: rope + bone toggle at (70, FLOOR+1.55, -9) (604-613), interact 'marrowAscend', enabled while inMarrow (652); doAscend requires held skull and p.z <= MZ+3.4; teleports to (11.8, 0.12, 34.6) (579-593), exitPalette. Both verbs ARM (_pendingDescend/_pendingAscend) and execute in the ticker after cullers (654-657) — same ordering contract as the ossuary, documented 545-550 (a teleport seen by forest cullers before the seal snapshots poisons the save/restore maps).
- Fog swap: enterPalette/exitPalette (508-524) ride game.fogColorTarget/bgColorTarget/fogTarget (never scene.fog directly); crypt palette 0x160611/0x0d030a/0.052; ticker re-asserts idempotently after respawn (685-689).
- Seal: syncMarrowVisibility (477-502), keep list camera/skull.root/marrowRoot/impact fx/isLight/world.lightRoot/userData.keepInMarrow.
- Pit presentation: pitGlow candle (11.8, 0.45, 36.2) + additive pitEmber plane (11.8, 0.03, 36.2) breathing once graveyardResolved (450-460, 622-631); one-time 3-knock announcement (634-639); recurring under-knocks within 12m (640-646). pitEmber visibility defers to any active seal (630-631).
- Contents: candle clusters at z MZ+3/9.5/16/22.5 (sprite stubs + 4 pooled descriptors, 336-356); 4 MOURNING STATUES at MX+{-2.2,2.3,-2.4,2.1}, MZ+{8,11.5,15,18.5}, scale 1.4, plinth colliders (359-384) — weeping-angel turn while unseen (750-757); after marrow:relicKept they HUNT: colliders collapse, dash 3.3 m/s while unseen (facing dot>0.38), touch d<0.85 → director.death (759-794); skull hit shoves 1.9*skullPower. ALTAR at (70, -4.45, 13.8) with skullPass collider (387-412); RELIC at (70, -3.76, 13.8) (402-405) + relicGlow candle. PRESENCE built by buildPresence (70-224, ported malform/stop-motion kit), guards at (70, FLOOR, 12.4) (416); skull passes THROUGH it (flinch only, 867-874); loom bands (876-884); yields when player dist < 1.15 → marrow:guardianYielded + fold + under-floor thuds (888-899). Relic take: skull outbound within rd<0.72, |Δy|<0.9, requires yielded (935-948) → dangle in jaw; refusal knock + relic flinch + guardian eye-flare otherwise (982-995). Escort beat: circling knocks then one last erupt between player and door, lets you pass (953-981). DARK EYES ephemeral wall-blink (422-441, 797-819).
- Relocating the entrance under the sealed mausoleum — position-dependent inventory (everything referencing surface coords): doDescend range check (555: 11.8/36.2/2.6), doAscend return pos (585: 11.8, 0.12, 34.6), pitGlow (450), pitEmber (459), announcement + recurring knocks (636-638, 641, 644), stoneGrind on ascend (592), pitInter bound to game.marrowPit.pit mesh (599-600), and game.marrowPit itself assigned by `if (x === 11.8)` in buildGraveyardLandmarks (outside.js:297). The underground hall (MX 70) needs no move. Also relevant at the sealed mausoleum site (15.6, 31.5): its 5 wall colliders (outside.js:266-270), grate collider 'mausoleumGrate' (358-359), plinth mesh (361-365, no collider of its own), tooth target, and relicGlow/candle at (15.6, 1.35, 32.05); the interior floor is bare ground plane — no pit exists there today. TRAP: stale comment at outside.js:294-296 claims "marrow.js collapses this collider" — it does not; the pit collider stays permanently raised and descent is the E-verb (marrow.js:647-651). The atmosphere `reserved` list (atmosphere.js:324-325) already clears both mausoleums (r3.5) and the pit (r1.9).

=== 5) THE GRAVEYARD BATTLE ===

- Armed on act entry (director._enterGraveyard 190-233): if not resolved, graveRitual = {credits:Set}, one dormant walker (-14,24) graveArena, two 'standing' gravePressure walkers (8,30),(16,16) (explicitly NOT wave bookkeeping — comment 227-231).
- Trigger: _updateGraveyardArena (637-707) — returns until player z >= 18 (645); then graveArena = {wave:0, pending:0, t:1.35}, wakeAll r60, skullScream, shake. Wave gate: waits for alive==0 && pending==0, one authored breath between waves (661-668, _graveClaimRecovery 0.7). Waves 1..3 = 4/5/6 'walker' spawns in 'wind' with graveRiseDur 1.08, staggered i*(0.58+wave*0.09), from the 8-site ring (676-699). After wave 3: _completeGraveyard('loud') (706).
- Alternative: 3 resonant-grave credits at any time → _completeGraveyard('ritual') (onGraveResonance 709-722). Each resonance also fires enemies.resonancePulse(pos, 8.2, 1.65) from outside.js:862-864 — stuns in radius, and a SECOND pulse on an already-stunned graveArena/gravePressure walker lays it to rest (enemies.js:1197-1215).
- "Before the three-key phase": no such phase exists anywhere in the code (no three-key strings, no key-count gate). Today's chain is: graveArena/ritual → _completeGraveyard(route) [flag graveyardResolved, gate ritualStage 3, ossuary.unlock(route)] → ossuary counterweight [flags ossuaryCleared+graveyardCleared, gate.openGate()] → climb out [ossuaryExited, act forest]. Anything inserted "before a three-key phase" would attach at these seams: _completeGraveyard (director.js:724-750) is the single funnel both routes pass through; ossuary.unlock (outside.js:1730) is where the yard's next door opens; gate.openGate call site (outside.js:2017) is the only thing that opens the surface gate. Wave count/composition is entirely in director.js:673-704; per-wave hook points are a.wave increments (674) and the completion branch (706). Restore/respawn parity must be maintained in director.js:195-215 and 1071-1087.

=== 6) DRAW / LIGHT BUDGETS — TRAPS FOR NEW SUB-AREAS ===

- pinLightCensus (world.js:982-1002, called main.js:262 with carriers [camera, skull.root]): every Light existing at boot is lifted into world.lightRoot and its `visible` property is REDEFINED (world.js:1051-1072) — always reads true, setter mutes color to black. Any Light ADDED after boot changes the census and recompiles every lit shader (the documented multi-second freezes, world.js:939-969). Rule for new areas: create real PointLights at BUILD time (the ossuary kennel lamp pattern, outside.js:1604-1613) and only ramp intensity; or use world.candles descriptors; or borrow via world.loanLight (world.js:1024-1036) — but only ONE loaner is reserved (main.js:256) and the Drowned Choir uses it (enemies.js:888).
- Candle pool: 8 pooled PointLights (world.js:1085-1097), retargeted to the 8 NEAREST descriptors every 0.4s (1103-1123), intensity ×35. Adding descriptors is free; only proximity wins slots — the ±70m district offsets are what let ossuary/marrow candles win the pool while occupied.
- Sealed districts hide by TOP-LEVEL scene children only (outside.js:1838-1852, marrow.js:488-502). Anything a new sub-area adds as its own scene root will be hidden by an active seal (safe default) — but anything that must SURVIVE inside a district needs userData.keepInOssuary / keepInMarrow, and world.lightRoot must always be spared (hiding it once dropped the census and re-triggered the recompile — comment outside.js:1830-1836; same law re-derived at underfalls.js:1300-1304).
- Back-district culling: game.graveyardRenderRoots = scene.children.slice(...) around buildGraveyard (outside.js:110-112) — includes routeRoot, marrowRoot, arrival hatch, keys, everything. Forest constructor builds backDistrictRoots = houseRenderRoots + graveyardRenderRoots minus graveyardLookbackRoots (outside.js:2940-2946); syncBackDistrictCulling hides them once past the gate (act forest/clearing/cave/mirror, or graveyard && !inOssuary && p.z > FOREST_GATE.z-0.45; outside.js:4623-4644). New graveyard sub-area roots built inside buildGraveyard's call tree join this retirement automatically; anything that must remain visible from the forest side must be pushed onto game.graveyardLookbackRoots (gate/fence/ground/arrival list at outside.js:1019-1026, 1918). Ticker-driven `routeRoot.visible = state.inOssuary` (2149) runs AFTER the seal each frame, and doClimbOut's restore-then-retire ordering (comment 1769-1775; crossedFarExit → syncBackDistrictCulling(true) at 2151) is a load-bearing sequence — copy it exactly for any new district exit that changes act.
- Forest detailRoots wake at act forest or graveyard && p.z > 31.5 (outside.js:4651-4657) — a new sub-area near the gate coexists with that pop threshold.
- batchStaticGroup (outside.js:26-81): merges static children per-material (ossuary shell → "four-ish draws", 2154-2156; car; bodies). Every animated/tickered mesh MUST set userData.noBatch or it is baked static — current noBatch set: mechanism, exitSlab, lidPivot, hatchChains, kennel bars/shutter/sliver/lampCore/cradle, niche minis, exitMouth (1255, 1305, 1447, 1474, 1535, 1585, 1599, 1608, 1703, 1812).
- Debris/stains are fixed pools asserted exact (grave debris cap 36, outside.js:611-615 "the pool's census is asserted exact" per key comment 432; enemy stain pool enemies.js:906-910). Never add per-event meshes.
- Regression coverage that will catch violations: tests/district-culling-regression.mjs (89 ossuary/marrow/graveyard matches; asserts zero unexpected visibility diff after ossuary round-trip, line 464), tests/grave-arena-regression.mjs, tests/regressions.mjs (59 matches), tests/playthrough.mjs (26).

Misc anchors: makeKey exported house.js (imported outside.js:10); ACT_SPAWNS/graveyard checkpoint director.js:8-21; AMBIENT_BY_ACT graveyard 0.46, FOG 0.034, colors 0x050b16 (director.js:28-61); skull.graveFear expression-only (director.js:140-141); world.rooms floorY drives ground in districts (level 'ossuary'/'marrow'), terrainHeightFn ignores them (outside.js:84-107).


---

# MAP: FOREST

FETCH map report — FOREST + CLEARING + WATERFALL (worktree C:\Users\Alex\Projects\fetch-aug15). All anchors file:line in that tree. Deterministic coordinates below were computed by replicating the spline RNG (mulberry32 seed 0x51ab, util.js:6-35) — they are exact for this build.

=== GROUND TRUTH NUMBERS (computed) ===
- Spline: 208 samples (1m arc steps). start (2.00, 44.00) — FOREST_GATE {x:2, z:43} + 1 (outside.js:13, 2800). end sample 207 = (-127.19, 199.63). Spline bbox x -127.2..2.0, z 44.0..199.6.
- fallenS() = floor(0.22L) = 45 at (-15.46, 84.30) (outside.js:3152). ravineS() = floor(0.5L) = 104 at (-60.12, 122.67) (2954). arenaS() = floor(0.72L) = 149 at (-93.39, 152.78) (2953). Kneeler authored s = floor(0.93L) = 193 (director.js:898).
- clearingCenter C = (end.x, end.z+22) = (-127.19, 221.63) (outside.js:5355-5357). Basin center (C.x, C.z+15.2) = (-127.19, 236.83). Falls veil plane z = 241.33 (C.z+19.70). waterfallBarrier z 241.18..241.98. Cave (underfalls) bounds x -137.19..-53.19, z 241.98..331.63.
- TRAP: clearingCenter x (-127.19) lies OUTSIDE the forest zone rect x -120..140 (outside.js:116) — the corridor's last ~12 samples are west of the forest zone; zoneAt there returns null until the clearing zone rect starts (z > C.z-24 = 197.63).

=== 1) FOREST SPLINE CORRIDOR (class Forest, outside.js:2784-5350) ===
Path model: authored heading walk, 26 legs x 8m, gauss heading clamp ±0.9 (2798-2807); resampled to 1m samples {x,z,tx,tz} (2809-2822). All movement is projection onto this polyline: project() (3170-3221, warm window ±40 with cold rescan when best hits window edge or d>40m; fractional s against adjacent edges; returns {s,i,lat,sample,fx,fz} — lat measured from fractional foot fx/fz, comment 3217-3219), posAt(s,lat) fractional (3223-3231), heightAt (3158-3168: mire dip lerp(-0.16,0) within |s-104|<3.2, else sin(x*.23)*sin(z*.31)*0.22 everywhere), contains(x,z) = z>42 || (z>30 && |x|>40) (3154-3156).

Width law (2891-2912): baseHalfW = lerp(2.4,1.5,smoothstep(0,0.35,i/L)) * (0.9+0.2sin(i*0.29)), + arena bulge 9*exp(-(((i-149)/14)^2)), + chain aisle 1.35*exp(-(((i-182)/26)^2)). halfW additionally adds pocket Gaussians (5.45@74 span10.5 side+1; 5.55@179 span10.5 side-1) and fork shoulders (max(base, base+shoulder*(separation+routeWidth+1.45))). clampPlayer walk limit = baseHalfW[i]-0.38, pockets open one-sided ONLY after flag `${pocket.flag}:latched` (3306-3314).

clampPlayer (3233-3441) — the corridor collider IS this function (world.postClamp, hooked 2950, later WRAPPED by underfalls installClamp, underfalls.js:1228-1275: forest clamp runs only when act !== 'cave'):
- guard: `if (this.game.act !== 'forest') return;` (3238) and `if (pos.z < 42) return;` (3239).
- mouth release: pr.s >= L-2 && beyondEnd > -0.5 && |mouthLat| <= halfW[L-1]-0.2 → no clamp (3250-3255). This is the one authored exit into the clearing.
- entry slam: entered at pr.s>4 → flag 'forestEntered', sealS = max(sealS, pr.s-6), _placeSeal(true), brushCrash+stoneGrind, lookWindow 5s (3257-3267).
- fall rescue: pos.y < -6 → teleport to posAt(max(pr.s, sealS+10)), y=0.6 (3270-3273). Separate death floor: director.js:379 forest y<-4 = death (fires first).
- mire (3275-3300): inMire when |pr.s-104|<3.08 && !player.swing; suction ramps _mireDepth to 1.72, death at depth>=1.48; velocity exp drag; audio thud+shake; swing (beginSwing) is the authored rescue. TRAP: mire sinking is entirely inside clampPlayer — in any act other than 'forest' the mire is just a walkable -0.16 dip.
- fork commit (3315-3355): forks[0] 'switchboard-braid' s48-64 (world (-17.81,86.16)→(-28.86,97.71)), commitDistance 6, separation 4.25, routeWidth 1.28, defaultSide -1; forks[1] 'washhouse-braid' s118-139 ((-70.85,131.66)→(-86.82,145.26)), separation 4.4, routeWidth 1.3, defaultSide +1 (2829-2847). Offset = separation*sin(pi*u)^0.82 (2966). Commit sets flags forestFork:<id>:left/right and a checkpoint on the chosen ribbon (2989-3014); closures raise parent + rejected-mouth tree rows with colliders growing from the dirt (rows built 4005-4055, collider max.y animated 4076-4121, forkClosedMaxY 3.2).
- seal frontier: self-heal `if (sealS+2.2 > pr.s+6) sealS = pr.s-SEAL_TRAIL` (3362; SEAL_TRAIL=10, 2781). minS = sealS+2.2; hard forward reposition via fractional posAt (3364-3372). Wall slide rebuilds from projection foot in the same frame (3373-3388). Fork ribbon re-resolve (3393-3407). Idle creep: after 32s stationary the frontier advances to pr.s-7 at 0.3 m/s; normal chase rate max(1.5,(target-sealS)*2.4) (3409-3419). Ground reconcile after XZ correction, ravine exempt (3420-3440).
- THE SEAL HAS NO COLLIDERS. _buildSealPool (4555-4577): one InstancedMesh, cap ceil(L*3.2)+64 = 730 trunks (cyl 0.16/0.3, M.bark). _placeSeal (4579-4621): cumulative rows every 1.2m, 4 trunks per row across halfW+1.2, h 4.5-8.5, rise 1.9-3.0s, never repositioned. Look-back sting: face frontier within 28m, dot>0.55 → sting+shake, once per fresh row (4686-4704). reseat()/recentre()/safeRespawnPad (3057-3151) rebuild frontier+rows for respawns; safeRespawnPad shifts pads off the fallen log (±3.6) and mire (±4.25).

OOB / walk-back-to-graveyard candidates (what the code actually permits):
a) ACT-GATE HOLE (the big one): once act === 'clearing' (or 'cave'/'mirror'), clampPlayer no-ops (3238) and NOTHING confines the player on z>42 terrain. heightAt returns the sinusoid for ALL x at z>42; the seal trunks are visual-only; the mire no longer sinks (act-gated); fork closures/dividers/story props keep their AABBs but the corridor walls don't exist. A player in the clearing can walk the entire 208m corridor backwards to the gate. The gate collider (outside.js:970-971, x 0.32..3.68, z 41.82..42.18, top 2.55) is permanently zeroed once opened (gate.t>0.32 → max.y=min.y, 1015) — so they re-enter the graveyard/house area while backDistrictCulling has all house+graveyard roots invisible (syncBackDistrictCulling 4623-4644: hidden for acts forest/clearing/cave/mirror; forest detailRoots also hidden outside forest act, 4646-4658). Result: walking on invisible districts. Acts never regress (director.js:359-362 order array), so no system re-seats them.
b) Gate-adjacent reversibility inside forest act: entered trips only at pr.s>4; sealS starts -10, so the first ~4m are freely reversible; z<42 exits the clamp's jurisdiction (3239). After a forest respawn (checkpoint spawn z=46, s≈2), reseat gives sealS = s-10 (negative) → minS<0 → the player can walk s→0 (z 44) then step to z<42, clamp gone, graveyard reachable in act 'forest' with districts culled invisible.
c) Clearing side gaps: the clearing bowl mesh spans x C.x±30, z C.z±27; the cliff colliders span only x C.x±30 at z C.z+20..24 (5413-5414). At |x-C.x|>30 there is NO wall — terrainHeightFn falls through to forest sinusoid (z>42), open walkable plane. Walking around the cliff's EAST shoulder (x > C.x+30 = -97.19… up to underfalls maxX -53.19) and north past z 241.98 enters the underfalls bounds: terrainHeightFn line 87-88 checks underfalls.groundAt FIRST, which inside bounds returns the nearest authored cave floor y (0..3.2) even off-route (underfalls.js:210-231 fallthrough `nearestSegment?.y ?? 0`). Pre-sacrifice the caveZone is disabled (underfalls.js:1522) so act stays 'clearing' and the underfalls clamp (act==='cave' only) never engages: the player walks on the invisible cave district's floors (renderRoots hidden until cave act, underfalls.js:1542-1546). caveHatch interact is inert (requires act 'cave' + waterfallTaken, 1218-1221). West side x < C.x-10 is outside underfalls bounds → endless sinusoid terrain; the under-floor plane (PlaneGeometry 320x400 at (10,-0.35,220), outside.js:3795-3798) runs out at x≈-150 → visible void beyond, but height function still returns ground (no fall).
d) terrainHeightFn seams: clearing window is |x-C.x|<30 && C.z-27<z<C.z+30 (194.63..251.63); forest fallback covers everything else at z>42; z<6 → 0; else graveyard sinusoid (84-107). The only real discontinuities are at the |x-C.x|=30 window edge (bowl ≈ -0.1 vs sinusoid ±0.22, sub-half-metre) and the underfalls-bounds edge z=241.98 (floor can be y 0..3.2 vs bowl -3.1 near the basin — a >3m step along the basin's back rim inside x C.x±? … basin only spans |x-C.x|<8.45 which is inside the barrier/cliff, so unreachable pre-sacrifice; reachable post-sacrifice when the barrier drops).

Swing/chain section (THE CHAIN, 2866-2887, _buildChain 4129-4300): seed knot s=65 (-29.64, 98.34) teaches the verb (releases ~8m from searchers-line pocket knot at s74); road knots s [165,173,182,191,200] = (-104.01,164.64), (-108.63,171.17), (-113.74,178.57), (-118.93,185.93), (-123.56,193.65). Pivot height 7.0 above centreline ground (law: 30·sin(elev)>14 lift threshold, comments 2871-2878); gaps <=9m so the anchored skull's lamp lights exactly the next knot. Targets id `forestChain:${index}` (seed uses index 5), radius 0.92, onHit guard `skull.mode !== 'outbound' || player.swing → 'continue'`, then skull.anchorAt(pivot,{swing:true,maxHold:5.8}) + player.beginSwing(pivot,{maxT:5.8}), flag `forestChain:<i>:latched`, next-knot creak cue after 0.24s (4188-4211). Targets never retire (4193). Art: 3 instanced draws (bark supports alternating sides, emissive rope lines 1.35, pale knots dodeca 0.19 emissive 2.4 with pulsing) + additive corona sprites (4259-4293); flora carves the whole chain runs [62,68] and [160,204] free of canopy blobs and knit branches (3546-3616). Ravine rope (the mandatory swing, 4913-4962): prop at posAt(108); anchor this.ropeAnchor (far.x+0.9, 1.4, far.z); target 'ravineRope' radius 1.1; pivot is (landing=posAt(111), y 6.9) NOT the rope; flag 'ropeLatched'; director disables target + drops the far-side checkpoint only once grounded past ravineS+3.25 with mireDepth<0.08 (director.js:776-787). Optional pocket lines (_buildOptionalRopes 5115-5349): targets 'forestRope:searchersLine' / 'forestRope:bellLine', radius 0.92, pivot landing+4.25y, maxHold/maxT 5.8; deadfall gate of 13 leaning poles along the pocket's base edge sinks -2.9 when `<flag>:latched` (5190-5218, ticker 5328-5347); discovery flag at 2.15m from secretPos. Pocket walk-space stays clamp-gated until latch (3309-3314).

=== 2) THE BIG FACETED "BOULDER" ===
It is not a rock. It is the fallen tree's ROOT PLATE: `const roots = new THREE.Mesh(new THREE.IcosahedronGeometry(1.25, 0), M.bark)` — outside.js:4724, child of the log group at local (-3.7, 0.1, 0), scale (0.55,1,1) (4725-4726). Icosahedron det-0 + bark canvas texture = the pentagon-ish faceted wood-grain blob in the screenshot; ~2.5m across (larger after the tear: hit 2 animates scale to (0.71,0.65,1.08), rot.z 0.62, y -0.35 — 4844-4851). Log group sits at fallenS=45, world (-15.46, 0.78, 84.30), yawed to lie ACROSS the trail (4729-4730); the shrub "hedge" walls flank at halfW+0.3..1.1 (3608-3634).
Collider mismatch (the deep-inside walk): the log's collision is 9 stepped AABBs along the log's own axis, centers u = -3.6..+3.6 (step 0.9), each only halfSeg 0.46 along-axis x halfThick 0.8 across x height 1.365 (LOG_R*1.75) — outside.js:4826-4834. The root plate's visual spans u ≈ -2.45..-4.95 and up to y≈2.1: beyond u -4.06 and above y 1.365 there is NO collider even before clearing, and the 0.8 half-thickness leaves ~0.45m of enterable visual on both faces. After the 3rd skull hit the whole log slides side→5.65 along its axis (roll.sideTo, 4900) and ALL log colliders are zeroed at |side|>=4.95 (4861-4864, flag 'treeCleared') — the root plate ends up ~1.95m off the trail centre, i.e. parked ON the walkable shoulder next to the shrub walls with zero collision; only the lateral clamp (baseHalfW-0.38 ≈ 1.4-1.6 here) stops the player, so the camera can stand ~1m inside the rock. Fetch target 'fallenTree' radius 2.55, 3 outbound hits, branch layers with skullPass colliders zeroed per layer (4868-4906). Handle: game.fallenTreeSetpiece {log, bole, roots, branches, target, colliders, roll, center, hits} (4907-4911).
(For contrast: the actual pentagon-faceted rocks are DodecahedronGeometry(1,0) instanced talus in the CLEARING dress, grey 0x687882, atmosphere.js:924-975 — none on the forest path.)

=== 3) CLEARING + WATERFALL INVENTORY ===
buildClearing (outside.js:5353-5562). C=(-127.19, 221.63). Zones: 'clearing' C.x±30, z C.z-24..C.z+20.4 (5359, registered BEFORE cave and forest so it wins zoneAt's first-match scan — world.js:290-298; note outside.js:2948-2949/116). Surfaces: dirt rect z..C.z+30 (5360). Death floor: y<-1.5 in clearing act (director.js:380).
- Ground bowl: PlaneGeometry 60x54 (40x36 segs), vertex formula identical to terrainHeightFn's clearing branch (5363-5377). CLEARING_BASIN frozen {centerZ:15.2, innerR:5.4, outerR:8.2, waterR:8.45, depth:3.15} (14-20).
- Water: stream plane 3x26 at (C.x-4, 0.06, C.z+2) rot.z 0.2 (5380-5384); plunge pool CircleGeometry r8.45 at (C.x, 0.08, C.z+15.2) (5388-5393); opaque murk cylinder body below (5397-5402). M.water map scrolls offset.y = t*0.25 (5405-5407). game.clearingPool / game.clearingBasin (5403-5404).
- Cliff: BoxGeometry(60,20,4) at (C.x, 9, C.z+22) (5410-5412). Colliders LEFT (C.x-30..C.x-3.2) and RIGHT (C.x+3.2..C.x+30), z C.z+20..C.z+24, y -2..20 (5413-5414). The 6.4m centre aperture is plugged by game.waterfallBarrier = collider (C.x±3.2, y -2..20, z C.z+19.55..20.35) (5454, alias bridgeBarrier 5455) until waterfallTaken zeroes max.y (director.js:980).
- Falls: the authored fall mesh (PlaneGeometry 6.2x19 at z C.z+19.9) is `visible = false` — retained only as game.waterfall progression/debug anchor (5416-5430 + bob ticker 5431-5434). The REAL falls are atmosphere's 'layered waterfall veil': ShaderMaterial, THREE ribbon widths of value noise (w1 f7.0, w2 f17.0, w3 f41.0) + two accelerating packet runs, glassy lip / basin foam, uniforms uTime + uSeal (atmosphere.js:977-1046; plane 6.75x18.9, 20x56 segs, z C.z+19.70). uSeal eases to 1 only when flags.waterfallTaken && act==='cave' at rate 0.62 (1048-1053); the stone-seal silhouette instanced rocks at z C.z+19.92 fade in with sealT (1144-1163, exported as game.underfalls.veilSeal). fallGlow PointLight (0xa8c0cc, 120, 26) at (C.x, 6, C.z+16) (5420-5422).
- Side cataracts: sideFalls = 6 sheets [x,width,height,lean] = [-14.4,3.3,10.8,-0.08],[-9.5,2.5,14.4,0.05],[-5.3,1.45,8.4,-0.04],[5.4,1.55,9.5,0.05],[9.7,2.45,15.8,-0.04],[14.7,3.15,12.1,0.07] at z C.z+19.66, instanced clone of the veil shader with uSeal pinned 0 (932-935, 1058-1069). NOTE heights in the array are the 2nd element (3.3, 2.5, 1.45, 1.55, 2.45, 3.15)? — no: instanced as (w=fall[1], h=fall[2]) i.e. widths 3.3.. are used as h*0.5+0.12 positions… (1062-1065: [x,w,h,lean], y=h*0.5+0.12, scale w,h — widths 3.3,2.5,1.45,1.55,2.45,3.15; heights 10.8,14.4,8.4,9.5,15.8,12.1).
- Cliff dress: talus facade instanced dodecahedra covering x ±29, y 0.4..18.8 at z C.z+19.25, skipping the |x|<3.35 aperture and cataract bands; 42 basin-shoulder rocks around the pool (skip |x|<3.0: "never counterfeit the bridge's collision language") (943-975). Edge crowns/buttresses + additive foam lips (1076-1139). None of this collides.
- Spray/mist/fireflies: one Points draw, 150 basin foam + 105 central mist + 84 cataract mist + 34 amber fireflies + 12/feeder pool (1167-1204).
- LEFT/RIGHT of the falls today: between the basin rim (|x-C.x|>~8.5) and the cliff line z≈C.z+19.25..20 there is a dressed shore strip ~21m wide each side: 3 feeder pools (local (-13.4,-2.8) r3.0x1.65, (12.8,-0.4) r2.7x1.5, (-7.2,5.7) r2.15x1.12; flat CircleGeometry water discs + terrace stone rings, 936-940, 1238-1317), two feeder streams flowing to the basin (addFeeder polylines 1287-1290), fern ring r 10..23.5 (1206-1225), moon-pale side groves at |x|>15.2, z -14.4..12.8 local — 2x18 trunks with branch forks and crown lobes (1319-1390). Stats bag game.clearingPlacehood (1392-1399). BOUNDS: only the cliff colliders (to x ±30) and the basin's own depth bound the sides; the east/west edges of the clearing (|x-C.x|=30) are OPEN (see OOB (c)). Vertical: nothing above y 20.
- Stepping stones: exactly TEN (not the 10 in the note's "(10)" — confirmed): bridgeZ = [7.35, 8.8, 10.52, 12.24, 13.96, 15.68, 16.55, 17.4, 19.12, 20.42] (5481); cylinders 0.72/0.9 x 0.5, 9 sides, x = C.x + sin(i*1.7)*0.34, start y=-1.4 (5482-5490). Comment block 5457-5480 documents the 8th (bank bridger), 9th (shelf lip 16.55), 10th (true first step 7.35). Rise: waterfallTaken schedules st.userData.rise=0.12 at 1.2+i*0.7s bank→falls wave with stoneGrind per stone (director.js:983-989, global beats); actual motion in main.js:1215-1216 (0.7 m/s up to y 0.12). Respawn re-asserts risen stones + barrier down + caveZone enabled + skull.vanish() if waterfallTaken (director.js:1103-1110). terrainHeightFn treats a stone as ground within 1.03m when st.y >= -0.35, top = y+0.25 (92-95).
- Sacrifice trigger: fetch target id 'waterfall', pos (C.x, 8, C.z+20.5), radius 3.4, enabled:false, onHit → director.waterfallTaken(), returns 'gone' (5493-5500). NO outbound-mode guard (unlike every other target). Armed by _enterClearing: `for (const t of g.world.fetchTargets) if (t.id === 'waterfall') t.enabled = true` + `this._gesturing = true` (director.js:258-259). The skull's ask: _updateGesture — while held, every 5-9s gazeAt the falls + whisper (925-936). waterfallTaken (974-991): _gesturing=false, flag 'waterfallTaken', checkpoint('clearing'), caveZone.enabled=true, bridgeBarrier max.y=min.y, splash+duck, stone wave; "the skull does not come back. no failsafe fires."
- The kept locket: shoreLocket appears 5.5s after waterfallTaken && keepsake at (C.x+9.3, ~ground+0.06, C.z+17.5), interact 'shoreLocket' → held locket added to skull.hold on LAYER_HELD, flag 'locketKept' (5502-5561).

=== 4) THE 6 ROUTE-SPANNING WATER CURTAINS (underfalls remake) ===
Built in atmosphere.js buildCaveDress, NOT underfalls.js: curtainAt(a,b) midpoints of route legs — main[0-1], main[1-2], main[8-9], main[9-10], main[11-12], secret[0-1] = 6 curtains (1751-1769), width = (a.w+b.w)+0.6, height 4.55, yawed perpendicular to the leg, sharing one InstancedMesh 'underfalls interior cataracts' with 7 side sheets (drops list 1735-1743, mesh 1799-1806) + 'underfalls displaced spray' points (1808-1820). Interaction with clearing geometry: NONE physically (no colliders, no lights of their own) and NONE visually — both batches are act-gated invisible outside act 'cave' by atmosphere's own ticker (caveNames set, atmosphere.js:61-91) AND by underfalls' cave-visibility culler names list (underfalls.js:1283-1288). First curtain sits at world (≈C.x+1, ≈C.z+26) — 4m behind the barrier, never crosses it. The crossing PULSES for first/last curtains are sprayZones 'entry-veil'/'hatch-veil' (underfalls.js:116-129, radius 4.2; player crossing → enemies.caveSpray, choir crossing → drench reveal, 1402-1437) — entry-veil's radius reaches z≈C.z+21.8 minimum, still behind the barrier.
TRAP (the one real clearing interaction): each curtainAt PUSHES A GLOBAL CANDLE DESCRIPTOR `game.world.candles.push({x:mx, y:my+1.35, z:mz, intensity:0.55, r:4.2})` (atmosphere.js:1757) unconditionally. The candle pool (world.js:55, 1085-1123) assigns its resident unshadowed PointLights to the NEAREST descriptors every 0.4s regardless of act; the clearing itself registers zero candles, so a player standing at the falls is nearest to the cave-side curtain candles (~7-10m through the cliff) and pool lights will seat inside the hidden cave — unshadowed, they can leak through the cliff box onto the falls/basin backside.

=== 5) ACT TRANSITIONS, AMBIENT/FOG, GATING HOOKS, FIRE SYSTEMS ===
Transition clearing→cave: zone-driven. director.update zoneAt scan with one-way order ['bedroom','house','basement','graveyard','forest','clearing','cave','mirror'] (director.js:356-363). caveZone = addZone('cave', C.x-10, C.z+20.35, C.x+74, C.z+110, -4, 14), enabled = flags.waterfallTaken at build (underfalls.js:1521-1522), armed by waterfallTaken (director.js:979). Registration order graveyard(outside.js:125) → clearing(5359) → cave(underfalls.js:1521) → forest(outside.js:116); zoneAt is first-match (world.js:290-298), so clearing beats cave in their 5cm overlap band and both beat the huge forest rect. _enterClearing (248-260): cancel forest arena, checkpoint, tension/dread 0, enemies.clear(), arm waterfall target + gesturing. _enterCave (262-323): checkpoint, dread 0.72, cave ecology (drips cadence [2.7,4.05,3.25,4.8], drownedCall every 8.4s, choir armed at z >= chapel.z+7 → enemies.beginDrownedChoir from 4m behind lower sluice). _leaveCave restores underfalls visibility snapshot (325-336). Cave spawn (getSpawn) = (C.x+0.35, C.z+23.35, yaw -2.9); clearing spawn = (C.x, C.z-16, yaw pi) (1175-1186). Hatch → enterMirrorRoom (993-1014).
AMBIENT_BY_ACT (director.js:47-57): bedroom 1.0, house 1.0, basement 0.9, graveyard 0.46, forest 0.54, clearing 0.68, cave 0.42, mirror 0.6. FOG_BY_ACT (28-34): forest 0.055, clearing 0.018, cave 0.055. FOG_COLOR_BY_ACT (35-43): forest 0x030b10, clearing 0x071821, cave 0x07141a — outdoor fog == background (BACKGROUND_BY_ACT 58-61: clearing 0x071821, cave 0x02080b). setAct writes fogTarget/ambientTarget/fogColorTarget/bgColorTarget; hard sets snap (114-137). Easing in main.js:1231-1235 (density damp 0.8, colors lerp dt*1.1). That per-act constant table + easing IS the whole "fog weather system" — there is no dynamic weather beyond the drifting cloud texture in the sky dome (atmosphere.js:133-174).
Where a pre-sacrifice gating layer (frozen falls until two fires lit) would have to hook — existing seams only:
- Arming: director.js:258 (the single line that enables target 'waterfall') and 259 (_gesturing). 
- Trigger: outside.js:5493-5500 onHit (currently unconditional; add-a-condition point). 
- Visuals: atmosphere waterMat uniforms uTime/uSeal (982, ticker 1048-1053) + sideWaterMat clone (1058-1059, separate uniforms, own ticker 1069) + fallGlow (outside.js:5420) + pool scroll ticker (5405-5407) + spray points uTime (1204). 
- Physical lock already exists: waterfallBarrier (5454) + caveZone.enabled (underfalls.js:1522).
Persistent fire systems that exist to reuse:
- Candle descriptor pool: world.candles {x,y,z,intensity,r} + _buildCandlePool/attachCandlePool/updateCandles (world.js:55, 1085-1123; resident unshadowed PointLights re-sorted to nearest descriptors every 0.4s; light census pinned, world.js:968-969). Descriptors are live-animatable (intensity written per-frame by owners).
- flameCircuit (game.flameCircuit, house.js:6348-6402): register/absorb; one-shot 'ateFlame'; extinguishes duplicate sources, seats 2 eye-socket embers on the skull, upgrades skullLight (62 / 12.5 / 0xd8c8a4).
- Guest flame (house.js:6321-6470): candle stand + flame mesh, glow descriptor born at 0; fetch target 'guestFlame' r0.9 (armed when voidDoor opens, 6480); ignition ticker (ignite.t, 6421-6440) runs heat down a brass line; flags voidDoorOpen/ateFlame.
- Basement pilot (buildBasementPilot house.js:2412-2612): wick + flame (visible=false until lit), pilotGlow descriptor intensity 0→animated (2512-2513, 2597-2603); target 'basementPilotFlame' r0.76 requires ateFlame; flags pilotLit/basementPilotUsed; fire runs UP the brass segments (2516-2536); furnace gates on pilotLit.
- Furnace/incinerator (house.js:2614-2800+): cone flame set + ember bed + breathing glow descriptor (2755-2760), flags fireRefused etc.
- No system named "standing flames" exists anywhere in src (grep verified); the closest standing lit fixtures are the hatch-cistern twin candles (underfalls.js:1222-1223) and per-curtain candles (atmosphere.js:1757). The wrecked-car headlight uses the world light-program idiom (world.js:1058 comment).

=== 6) GROUND-EMERGING ENEMIES ===
Kinds (enemies.js KIND, 7-23): 'walker' (h2.05 r0.3 hp1 stun2.8), 'resident' (hp Infinity), 'kneeler' (h4.4 scale2.4 hp Infinity) — plus the special 'choir' (DROWNED_CHOIR, 29+, water-borne, NOT ground-emerging, spawned only via beginDrownedChoir 1008-1055).
Ground emergence is the DEFAULT ENTRANCE for every kind, not a special enemy: spawn(kind,x,z,state='stalk',yHint=3) (963-1002) sets graveRiseDur=1.15/graveRiseT unless state is 'dormant' or 'standing' (994-1000; walkerRise audio outside graveyard). The rise pose (1840-1852): compressed drag-out — mesh.y -= remaining²*1.35, scale.y 0.34→1 (or riseSquashOnly: 0.06→1 squash with no sink), riseFrozen lets an author own the clock (comment names "the nursery" = the music-box walker, director.js:532-552: dormant walker at (-11.2, 5.3) storey 4.6, graveRiseDur 11.5, riseFrozen, riseSquashOnly, clock driven by the music box; rise reverses when the box closes).
Spawn interface as used by director.js/outside.js: enemies.spawn / enemies.clear(pred) (1153-1158) / enemies.wakeAll(x,z,r) (1180-1186, dormant→'wind' within r) / resetGraveClaims / beginDrownedChoir/endDrownedChoir/drownedChoirHear/caveSpray.
Placements: graveyard entry — 1 dormant walker (-14,24) + 2 'standing' Standing-Kind (8,30),(16,16) gravePressure (director.js:218-232, cleared on forest entry 240); graveyard waves — 8 fixed sites, 4/5/6 per wave, state 'wind', graveRiseDur 1.08, stoneGrind+walkerRise per body (676-700); forest chasers — 'tree-cleared' stalk walker at _lastIdx+26 after 6s, 'rope-latched' wind walker at _lastIdx-8 after 4s, one-shot chips _chaser1/_chaser2 (789-814); forest arena — waves n=3,4,4 'chase' walkers on ring r 19-23 around posAt(149), staggered 0.9-2s, e.forestArena, cleared→flag arenaCleared + skull stage 4 (820-882); forestCompany — noise economy invitations, cap 2 active, spawn at heardS+12+loudness*8 lat 1 (1127-1163); kneeler — dormant at posAt(min(L-4, max(193, _lastIdx+11)), lat 2.2), mesh.rotation.x=0.5, wakes only if d<16 && player.noise>0.6 (895-922); ossuary resident 'standing' (outside.js:1966); basement standing walker (house.js:1339); boot warm-up spawns all three kinds dormant then removes them (main.js:498-508).

=== TRAPS SUMMARY ===
1. Act-gated corridor: every forest containment (clamp, seal push, mire sink, fall rescue) requires act==='forest' (outside.js:3238) — in clearing/cave acts the whole z>42 plane is open, both back to the graveyard (invisible, culled) and around the clearing's uncolliding east/west edges.
2. The seal is not a collider; the gate collider never re-rises after opening (outside.js:1015).
3. Root-plate "boulder" has partial-then-zero collision (colliders are the log's 9 boxes only, zeroed at treeCleared; visual parks on the walkable shoulder).
4. terrainHeightFn priority: underfalls.groundAt wins over the clearing bowl inside cave bounds z>=C.z+20.35 (outside.js:87-88); off-route positions inside bounds get nearest-floor heights (underfalls.js:230 `?? 0` fallback), enabling invisible-district walking pre-sacrifice.
5. 'waterfall' target onHit has no outbound/mode guard (outside.js:5496) and is armed unconditionally at clearing entry (director.js:258) — the exact two lines a frozen-falls gate must own.
6. Curtain candles leak into the global candle pool from the clearing side (atmosphere.js:1757; pool is act-blind, world.js:1103-1123).
7. clampPlayer's z<42 early-out (outside.js:3239) plus entered-at-s>4 makes the first metres reversible; post-respawn sealS regression (3068, 3362) can re-open the way back to the gate.
8. Forest zone rect x -120..140 does not contain the corridor's final samples (x to -127.2) — zoneAt returns null there until the clearing rect takes over; anything keyed on zone==='forest' is blind in that stretch.


---

# MAP: GATES

All source read. Here is the report.

FETCH TEST-GATE MAP — C:/Users/Alex/Projects/fetch-aug15 (v0.5.0-intruder; package.json: `npm test` = autotest && regressions && smoke; `npm run test:playthrough`; `npm run test:gpu` = tests/render-perf.mjs)

=== SHARED HARNESS (tests/lib/harness.mjs) ===
- System Chrome on real GPU, NEVER swiftshader (harness.mjs:1-2, comment: "timing assertions on software GL measure the harness, not the game"). launchBrowser (63-74): headless, `--use-angle=d3d11`, `--enable-webgl`, `--ignore-gpu-blocklist`, `--mute-audio`.
- Chrome resolution (23-33): FETCH_CHROME env, then Program Files paths. Playwright-core resolution (15-21): FETCH_PLAYWRIGHT env → require.resolve from ROOT → legacy npx cache path.
- PORT = FETCH_PORT || 9200+(pid%600) (35). ensureServer (40-61) reuses a live server on that port or spawns `node serve.mjs PORT` with FETCH_DEBUG=1.
- openPage (76-87): collects pageerror + console.error into `errors` (favicon filtered). shotPath → tests/shots/, resultsPath → tests/results/.

=== 1) tests/playthrough.mjs — the completability gate, 49 beats (memory says 48; source has 49 `beat(` calls) ===
Framework: single page `?test=1&mute=1`; waits `window.__FETCH.ready === true` with 300000 ms timeout (line 15; comment 13-14: boots take 24-45 s when a concurrent agent's battery owns the GPU — wall-clock slowness is not failure). Whole run is ONE page.evaluate (21-810). beat(name, ok, extra) (25-28) pushes {name, ok, extra} to log and console.logs `[beat] PASS/FAIL` (echoed live via page console listener, 16-19). No per-beat timeout — every wait is a bounded for/while loop with a time or iteration guard. Line 31-32 TRAP: `F.stepWith` is monkeypatched to always pass render=false ("rendering every walk step is what killed the last run") — sim-only stepping. snap(name) (29) renders once and reads canvas via toDataURL jpeg 0.75; only 4 snaps remain (00-wake, 01-kitchen 313, 02-basement 349, 03-graveyard 450); five later snaps are REMOVED with comment "sync canvas readback wedges headless GPU after long render-free stretches" (709, 736, 762, 796, 805). Results → tests/results/playthrough.json; shots → tests/shots/play-*.jpg; exit 1 on any failed beat or page error.
Bot verbs: aimAt (34-40) writes player.yaw/pitch + _sync(0); throwAt (41-48) press → hold min(2.6, dist/20+holdExtra) → release; waitHeld (49-53) steps until skull.mode==='held' (max 4.5 s); walkTo (54-84) 0.1 s steps, success <0.7 m (relaxed 1.5 m at timeout), and mid-walk in act 'house' auto-stun-throws the Resident when <3.4 m and |Δy|<1.8 (63-77); useAt (85) aim+interactPressed; fendOff(radius=7.5) (90-98) stun-throw the Resident (uses director._liveResident?.()||director.resident); fightNearbyWalkers(range=12) (99-109) up to 10 throw cycles.
Beats in order (name — what it drives / asserts):
ACT 0 bedroom, THE NEW OPENING (114-224; Alex's quote block 115-121):
1. wakes-empty-handed (125): skull.mode==='gone', skull.root.parent===null, !flags.has('skullArrived'), bedroomArrival.state==='searching', skull._handPose.g>0.5.
2. bell-hidden-until-found (162): after E at the bell's exact hiding spot (useAt 10.42,3.7,2.16) BEFORE the find: !A.bellFound, !A.bellRung, bedroomProps.bell.group.visible===false, mode still 'gone'.
3. the-room-answers-search (169): A.searchedCount===9, every g.bedroomSearch[id].done, A.bellFound, A.state==='bellFound'. The nine searches with exact stand/aim coords (135-168): curtains S6 (5.99,5.2,5.84), dresser S1 (9.35,4.12,5.2), nightstand S7 (8.66,4.14,4.26), covers S3 (10.15,4.25,3.75), wardrobe S2 (5.15,4.7,4.55), art S8 (4.19,5.08,2.35), latch S9 (4.09,5.17,1.45), rug S4 (10.65,3.64,2.35), floorboard S5 (10.6,3.64,2.16). Search retries up to 3 (137).
4. bell-rung (182): A.bellRung && state==='called' (E at 10.42,3.7,2.16, 3 tries).
5. the-window-broke-inward (188): within 16 s, bedroomGlass.broken, collider collapsed (max.y===min.y), pane.parent===null, shards.visible.
6. it-landed-in-your-hands (195): skull.mode==='held' within 4 s.
7. it-did-not-take-the-key (197): skull.carry===null, !flags.has('gotBedroomKey'), fetchTarget 'treeKey' enabled and object.parent===g.scene. (Alex: "Make sure it doesn't get the key on the way in.")
8. throws-wait-for-the-settle (220): press during introFlicker does nothing (stays held); after flicker settles + skullArrived, same press → mode 'outbound' sampled ON the press step (216); A.state==='done'. Scratch throw aimed at near wall (9.0,4.6,0.4) — never the window (206-208).
9. key-fetched-from-tree (232): from window aperture (walkTo 7.2,4.6) throwAt (7.2,5.7,8.2) → carry.id==='bedroomKey', re-held.
10. bedroom-door-unlocked-by-throw (239): throwAt lock (4.3,4.7,0.8) → flags 'bedroomOpen'.
ACT 1 house (241-347):
11. reached-landing (243): g.act==='house' at (-1,2.5).
12. stair-key-fetched (250): nursery route, throwAt wall hook (-11.55,5.7,4.6) → carry 'stairKey'.
13. stair-door-unlocked (257): throwAt (1,4.7,0.2) → flags 'stairsOpen'.
14. void-door-points-to-window-relay (268): throwAt (4,4.75,-7) → flags 'voidDoorTried' && !'voidDoorOpen'.
15. window-relay-mooring-catches-real-throw (282): in living-room aperture, fendOff(12) first (comment 274-275: the ~10 s held anchor is the one window the bot can't defend), yaw=PI/2, pitch=-0.015, hold → skull.mode==='anchored' && anchor.puzzleId==='windowRelay'.
16. window-return-bell-opens-void-door (294): scripted held-walk legs (3.48 s moveZ:-1, 3.72 s moveX:-1, 1.82 s moveZ:1 run) → flags 'windowRelaySolved' && 'voidDoorOpenedByRelay' && 'voidDoorOpen'.
17. the-skull-ate-a-flame (305): throwAt (5.3,4.95,-7) → flag 'ateFlame'.
18. reached-kitchen (312): |x-9.5|<2 && y<0.5.
19. boards-broken (331): every g.boards thrown until userData.off → flags 'cellarOpen' && !dead; after each board, stun Resident if <7 m (323-328).
20. resident-came (335): director.resident exists, kind==='resident', registered in enemies.list; extra: director.residentPressure.
ACT 2 basement (342-448):
21. in-the-basement (348): E cellar door at (9,1.1,2), descend → act==='basement' && !director.resident (door-slam beat).
22. carried-the-fire-down-and-lit-the-pilot (362): throwAt pilot wick (3.35,-2.12,5.55) → flag 'pilotLit'. Comment 350-356: pilot COLD until guest flame carried down; fixture on clear floor against south wall west of return-stair foot (moved out of the stair wedge).
23. pump-restores-the-furnace-draft (392): at (-12.62,-6.8) aim pumpGallery.cradle world pos, hold → anchored puzzleId 'pumpGallery', then held-walk legs (2.45 s hold, yaw PI/2, 1.12 s moveX:-1, 1.55+0.35 s moveZ:1 run) → flags 'pumpGalleryLatched' && pumpGallery.gateOpen.
24. archive-lamp-hold-opens-the-draft (409): aim (-16.25,-0.88,4.72), hold 3.2 s (draftHold.required is 2.6 s — comment 402-404) → flag 'archiveDraftOpened'.
25. fire-refused-the-skull (424): E incinerator mouth (10.71,-2.1,-1.52), throw into it (11.0,-2.1,-1.5) → flag 'fireRefused' && skull re-held.
26. hatch-key-fetched (429): throwAt ash (10.5,-2.65,-1.5) → carry 'hatchKey'.
27. hatch-unlocked (445): throwAt padlock (-10,-1.25,3.6), 3 tries interleaved with fightNearbyWalkers(8) → flag 'hatchUnlocked'.
28. out-in-the-graveyard (449): E hatch (-10,-0.9,4.4) → act==='graveyard'.
ACT 3 graveyard (452-658):
29. the-locket-answered (458): throwAt tree glint (5.2,7.55,13.85) → flag 'keepsake'.
30. graveyard-funeral-resolved (552): the arena loop (462-551), guard<240. graveThrowPoint strafes (alternating graveStrafe ±1) and flees the nearest committed graveArena body during return, with perimeter clamps x∈(-16,20), z∈(10,39) (484-487, 515-518); honors the strike telegraph (evade while state==='strike' && d<2.5, 509-525); prioritizes urgent graveClaimed <5.5 m, then resonantGraves with ≥2 caught in 8.2 m radius (531-542), then nearest threat/downed (2.8 s execute window comment 543-545). Asserts flag 'graveyardResolved' && !dead.
31. mausoleum-opens-required-ossuary (590): clearMausoleumShell(15.6,31.5) and (-14.6,34.2) (559-572: combat can end inside a shell OR one capsule radius along its side wall — old narrow "inside" test drove into the east wall forever); open-yard landmark route 7.5,24.5 → -4.0,23.5 → -11.5,25.0 → -14.6,31.6 → -14.6,33.4 (routes around open grave at (-7.2,27), 577-580); walk into throat toward (-14.6,34.8) → flags 'ossuaryEntered' && g.ossuary.inOssuary.
32. ossuary-counterweight-catches-the-skull (602): baffle slalom -68.0,-3.4 → -68.0,-1.2 → -72.0,3.4 → -72.0,5.6 → -68.0,10.8 → -68.0,12.9 → -70.0,15.0; aim (-70,-2.85,16.25), hold → anchored puzzleId 'ossuaryCounterweight'.
33. held-counterweight-opens-gate-and-exit (611): after 1.9 s hold: flags 'graveyardCleared' && 'ossuaryCleared', graveyardGate.opening, AND exitCollider still CLOSED (max.y>min.y). Comment 606-610: the collider now tracks the sinking slab; the old assertion (open at solve time) asserted a walk-through-visible-stone bug as correct.
34. exit-slab-sank-and-opened-the-way (619): within 4 s exitCollider collapses (max.y===min.y).
35. ossuary-exits-up-the-shaft-climb (651): THE CLIMB (622-650) — flight A target (-68.2,23.6) while z<23.0, then flight B (-72.45,24.65); climbPeakY must rise (>-1.16) proving real ground contact; atHatchTop() = y>origin.floor+3.05 && x<origin.x-2.0 && z>origin.z+33.9; then E into the mouth at (origin.x-2.45, floor+5.43, origin.z+34.75) → flags 'ossuaryExited', !inOssuary, no ossuaryResident left un-dying; extra logs exitT, gateOpening.
36. entered-the-forest (658): walkTo (2,45) → act==='forest' (comment: sealed-path beat proves 'forestEntered' later — note flag name never actually asserted).
ACT 4 forest (660-735): walkPath(toS) drives along f.project/f.posAt(s+6), auto-stunning walkers <9 m (run: 4 m) (662-684).
37. fallen-tree-cleared (695): up to 7 throws at fetchTarget 'fallenTree' object.position → flag 'treeCleared'.
38. path-sealed-behind (696): f.sealS > 0.
39. rope-launch-crossed-ravine (708): throwAt g.forest.ropeAnchor (holdExtra 0.8); flew = projection s ≥ f.ravineS()+1 within 7 s; && flag 'ropeLatched' && !dead.
40. arena-survived (729): walk INTO the ambush at f.arenaS()-2; loop guard<300, stun threats <8 m first, execute downed (iframes<=0) → flag 'arenaCleared' && !dead.
41. reached-the-clearing (735): sprint to 0.82·length then quiet past the Kneeler ("never re-throw at the downed", 731); walkTo clearingCenter.z-10 → act==='clearing'; extra dumps enemies + g.spawnLog tail.
ACT 5 clearing/cave (738-796):
42. skull-given-to-the-waterfall (743): throwAt (C.x, 8, C.z+20.5, 0.9) → flag 'waterfallTaken' && skull.mode==='gone'.
43. bridge-rose (745): after 10.5 s all g.bridgeStones y > 0.05 ("ten stones now").
44. it-left-the-locket (754): around plunge pool along EAST rim (C.x+8.7, C.z+17.1), E at (C.x+9.3,0.1,C.z+17.5) → flag 'locketKept'.
45. inside-the-cave (761): through the fall (C.z+21) → act==='cave'.
46. crossed-the-underfalls (780): g.underfalls.layout.main must have ≥13 nodes, mainLength>110, chambers ≥5 (underfallsRouteReady); walk every main node from index 2 with plain walkTo; all legs true && !dead. Comment 764-767: old cave was a straight tube; exercise every authored leg instead of bearing through chapel/sluice walls.
47. in-the-mirror-room (795): stand OFF-center at caveEnd.x-1.2 (straight up beyond pitch clamp, 792), E hatch (caveEnd.x,3.7,caveEnd.z) → act==='mirror' && finale.active.
ACT 6 (798-807):
48. the-walls-did-not-stop (806): squirm loop up to 90 s (moveX alternating ±0.4) until finale.phase==='end'; assert closed && finale.half<=0.38.
49. ended (807): flags.has('ended').

=== 2) tests/regressions.mjs — 15 scenarios, 97 runtime checks (82 content + 15 auto 'zero browser errors') ===
Framework (40-76): scenario(name, evaluate) opens a FRESH page per scenario (`?test=1&mute=1`), waits __FETCH.ready && window.__game 300 s (same GPU-contention comment 51-53), runs one page.evaluate returning {checks:[{name,passed,details,skipped}],diagnostics}; record() prints PASS/FAIL/SKIP; after each scenario an automatic check 'zero browser errors' (62-66); an exception becomes failing check 'scenario completed without exception' (70). Report → tests/results/regressions.json. Declared assumptions (16-20): window.__game is the test-only white-box companion to __FETCH; hard act teleports are SETUP only — progression assertions use zone updates or real movement; set-piece callbacks invoked exactly as the skull-hit dispatcher invokes them.
Scenarios and their checks, grouped by area:

SKULL / PRESENTATION — 'skull-growth-law' (79-275), 12 checks: default query ships the continuous anatomical shell (variant 'e', source 'real-shell', triangleCount<=budget; comparison variants incl. v0 opt-in, 106-108); shipping rig complete (jaw, jawMount, ≥2 sockets, eyes, 6 stageSets); a held throw draws one finite depth-tested tether (depthWrite false, opacity>0, finite positions); ordinary release catches into animated hand-span settle (_catchFx) hiding tether, _grip<0.2; ordinary catch adds no hitStop; settle ends in exact cradle (root at 0,0,0.02 within 1e-4); requested growth WAITS while held (setStage counted via monkeypatch, applications===0); waits while outbound on-screen (NDC test); applies exactly once when unseen (behind camera); not re-applied later; gone skull accepts pending stage once then stable; stage-5 growth keeps sockets/eyes visible and hides ≥5 stage5Hide meshes. TRAP 87-94: fresh boot is skull-less; scenario hard-teleports to 'bedroom' to invoke the arrival-complete contract before testing grammar.
PROGRESSION / CLEARING — 'progression-gates' (277-398), 5: entering an earlier authored zone cannot move story backward (drops player into an earlier zone center at act 'graveyard', director.update, act unchanged); visible plunge-pool water covers the full basin (clearingPool.userData.radius ≥ clearingBasin.outerR+0.2); cave story zone DISABLED until waterfallTaken (caveZone.enabled false, zoneAt(center)!=='cave'); simulated forward run from clearing cannot reach cave (dies in unbridged pool OR stops before bridgeBarrier.min.z-0.1 — the authored outcome is physical, 366-369); caveHatch interactable independently rejects use even with an illegally injected g.act='cave' (378-381).
INPUT/HUD/RENDER — 'input-and-no-hud' (400-499), 4 (1 may SKIP): crosshair is the wordless 4px dot (textless, dataset target/skull); clearKeys converts a lost held button into exactly one release edge (SKIPs if g.input not exposed); a stale negative render timestamp cannot invert camera projection (fov 70-75, projection[0]>0, [5]>0 — the giant-wedge/fisheye bug, 448-450); world lantern and held cradle stay isolated across holdNow (skullLight.layers.mask===1, all hold meshes mask 1<<2, camera mask (1<<0)|(1<<2), drawCalls in (0,700)).
GRAVEYARD — 'graveyard-attack-tokens' (501-595), 7: early wave owns exactly one persistent claimant; Standing pressure figures never steal a wave token; ordinary footsteps (director.onPlayerStep('grass')) preserve the committed token and _graveClaimRecovery; a committed strike keeps its token when another body becomes nearer; quiet stun (enemies.resonancePulse) releases the token with recovery>0 and no immediate reassignment; footsteps preserve the post-stun recovery breath; a new attacker claims only after the recovery breath (never a gravePressure figure).
OSSUARY — 'ossuary-climb' (597-679), 4: mausoleum throat swaps into the district (g.ossuary.unlock('regression'), stand at ritualMausoleum (m.x, m.z+0.4)); resident is the real Standing Kind spared by the district seal (standing===true, mesh.userData.keepInOssuary, lightRoot visible); forced restore seats slab+hatch+arrival from exitT ALONE (solved=true; exitT=1 → exitCollider collapsed, lidPivot.rotation.x>1.6, arrival.pivot.rotation.x<-1.7 — the ONE number the director restore uses); the far exit is a grounded climb (waypoints OX+1.8/OZ+8.6 … OX-2.45/OZ+34.65, monotonic y with regressed===0, peak>floor+3.0, E-mouth exit → act 'forest').
'ossuary-kennel' (681-732), 4: cradle catches an outbound skull through the bars (anchored, puzzleId 'ossuaryKennel'; cradle at OX-4.75, FLOOR+1.02, OZ+12 thrown from OX-2.6); held weight raises the shutter and latches (flag 'ossuaryKennelSolved', shutter.position.y>FLOOR+2.2); skull returns home through the bars; the bars STILL stop the player after the solve (x stays > OX-3.8).
MARROW — 'marrow-descent' (734-905), 13: pit mouth offers its verb once g.flag('graveyardResolved') (marrowPit inter enabled, collider open); descending is USED never walked-over — stand (11.8,0.05,34.85), pitch into mouth, E → M.inMarrow, flags 'marrow:entered' + 'marrow:witnessed'; marrow wears its own palette (fog eases in — sRGB byte proximity to 0x160611 within ±14/channel, NOT exact hex, 765-770); after the introduction the presence guards the altar (P.position.z ≈ origin.z+26-3.6 ±0.5); a thrown skull passes THROUGH the guardian and comes home (M.yielded stays false); walking into the loom makes the guardian yield through the floor (flag 'marrow:guardianYielded'); the relic rides home in the skull jaw (flag 'marrow:relicKept', jaw.children grew; relic at floor+1.24, z+26-2.2); after the theft an UNOBSERVED statue dashes (advanced>1.2 in 1 s; statues re-seated at userData.home first, others parked at far end or four simultaneous hunters end the test — 826-831); a watched statue freezes mid-hunt (<0.05 moved); a skull hit staggers the hunter back (>0.8 shove); letting a mourner reach you is the end (g.dead); respawn stands you back up inside the marrow with mourners reposted; way-up toggle (E at origin.z+2.2) surfaces beside the grave and restores the fog (fog !== 0x160611, act 'graveyard'). Diagnostics: flag 'marrow:escorted'.
FOREST — 'forest-mouth-boundary' (907-964), 3: final forest side authoritative after a huge lateral breach (clampPlayer pulls |lat| ≤ half-0.15; the s=last inheritance bug); the narrow forward mouth still releases into the clearing (no clamp displacement); forest post-clamp never rewrites the distant mirror room (teleport 'mirror', clampPlayer must not move player).
'forest-chain' (966-1022), 3: THE CHAIN IS NEVER THE ONLY ROUTE — s160→205 walkable on foot with no throws; all six 'forestChain*' fetchTargets exist and stay enabled after use; death mid-chain (link.s===182, onHit directive 'anchor', mid-swing death) drops the rope and restores a grounded on-trail pose (|lat| < halfW+0.5).
'forest-checkpoint' (1024-1188), 4: a failed rope attempt stays retryable (ravineRope onHit 'anchor'; callTap releases BOTH halves of an active rope in the same fixed step even with throwHeld, 1058-1062; rope stays enabled); successful crossing spends the anchor (enabled false) + flag 'ropeLatched' + far-side g.checkpointPose with s ≥ ravineS+1; death on the wrong side restores the EXACT far-side checkpoint (<0.01 m) with rope still spent; restored pose can continue forward without reusing the rope. TRAP 1037-1040: hard placement must seed forest._lastIdx (spline warm-start) to match a walking player.
CLEARING/END — 'waterfall-respawn' (1190-1264), 2: the bargain records flag+opens progression+removes skull (directive 'gone' → skull.vanish() mirrors Skull._checkTargets, 1204; caveZone.enabled true, bridgeBarrier collapsed); death+respawn+callTap cannot undo the waterfall loss.
'finale-contact' (1266-1388), 5: TRAP 1273-1278 — complete the bedroom arrival FIRST via teleport('bedroom') BEFORE vanish(), else teleport's instant-completion (keyed on missing 'skullArrived') would resurrect the skull; mirror-room throw/call inputs stay silent (mode 'gone', phase 'still'); player retains movement+look before the walls; closing is monotonic to a visible controllable 'contact' phase (half ≤ 0.371, exactJaw rotation.x>0.3, not frozen); impossible recall, hard black, catch, localized gasp IN ORDER (audio spy order exactly skullMoanStart, skullMoanUpdate, skullMoanStop, catchThud, gasp; phase 'end', flag 'ended', frozen, endTag '', drawCalls<700); mirror player+camera co-located far outside forest jurisdiction (x,z > 490).
BEDROOM ARRIVAL (the three arrival traps, header 1390-1396; "Alex: Make sure it doesn't get the key on the way in") —
'arrival-never-takes-the-key' (1398-1488), 7: room boots skull-less behind strong glass (collider open, pane parented); rug-then-floorboard find surfaces the bell (S4 at stand 10.4,1.25 aim 10.65,3.64,2.35; S5 stand 10.3,1.35 aim 10.6,3.64,2.16 — every E a real input frame); bell answers with the summons (state 'called'); summons runs to the settled catch (state 'done', held, no introFlicker); the inbound flight ran in mode 'gone' (trace sampling — no target can fire); window burst inward and STAYS burst; the arrival never takes the key (carry null through the whole trace, treeKey enabled, in scene, !'gotBedroomKey').
'restart-before-arrival-keeps-the-room-skull-less' (1490-1561), 5: after 2 honest searches + F.pause + F.restartCheckpoint: room still skull-less (state 'searching'); searches PERSIST (world truth; dresser drawer z ≈ 5.235-0.24 ±0.02); glass still whole and strong; bell still findable (searchedCount 4); still re-ringable.
'restart-after-arrival-keeps-skull-and-broken-window' (1563-1606), 4: teleport hands over the post-arrival room; restart keeps the skull in the hands (no flicker, state 'done'); restart keeps the broken window broken; the bell verb stays spent (interactable id 'bedroomBell' enabled===false).
ROUTE-RESTRUCTURE BLAST RADIUS: bedroom coords are pinned in THREE places (playthrough beats 1-8, arrival-never-takes-the-key, restart-before); ossuary geometry (origin-relative waypoints, exitT contract, mouth E point, kennel pocket, counterweight puzzleId) pinned in playthrough beats 31-35 + ossuary-climb + ossuary-kennel (+ tools/probe-ossuary.mjs); graveyard token economy + arena perimeter in graveyard-attack-tokens + playthrough beat 30; marrow entry pose (11.8,0.05,34.85), altar/relic offsets, fog hex in marrow-descent; clearing pool/bridgeBarrier/caveZone gating in progression-gates + waterfall-respawn + playthrough beats 42-45.

=== 3) autotest — 26 checks; runner tests/autotest.mjs, checks live in src/main.js runAutotest (main.js:1568-1705) ===
Runner: opens `?autotest=1`, waits up to 180 s for document.body.dataset.autotestResult 'pass'/'fail', prints per-check from dataset.autotestDetails JSON, writes tests/results/autotest.json, exit 1 on non-pass OR any browser error. Suite crash fallback writes check 'suite-crashed' (main.js:1713-1716).
Checks (main.js line): 1 boot-ready (1578); 2 act0-boots-skull-less (1583, mode 'gone', root unparented, !'skullArrived' — teleports still work via _completeArrivalInstant contract, comment 1579-1582); 3 hud-prints-no-words-during-play (1586, all #hud text empty except .sr-only); 4 teleport-grants-the-skull (1598, held + 'skullArrived'); 5 throw-enters-outbound (1603); 6 quick-call-enters-return-on-the-next-fixed-step (1606, mode 'returning' && snapReturn===true — release IS the recall, lands hot); 7 skull-returns-and-is-caught (1608); 8 called-skull-comes-home (1615, bare-tap zip); 9 skull-cannot-be-lost (1620, setSkull to y=-120 outbound → held within 4 s); 10 tree-key-rides-in-the-teeth (1626, carry 'bedroomKey'); 11 key-unlocks-the-bedroom-door (1646 — delivered by AIMED live-camera throw, never teleported past the aim; outbound guide steering follows the live camera, comment 1628-1631); 12 door-collider-collapses-when-open (1650, setOpen toggling max.y===min.y); 13 stairs-report-ground-height (1660, groundHeightAt(2,-6,3) in (0.5,3.6)); 14 skull-stuns-a-walker (1672, camera aims +x so guide steering helps, 1665); 15 stunned-walker-pops (1678, second hit after 0.4 s immunity window → 'dying' or removed, flag 'firstPop'; pop is a physical 0.62 s corpse tumble, 1676-1677); 16 popped-walker-is-gone (1680); 17-24 teleport-bedroom/house/basement/graveyard/forest/clearing/cave/mirror (1686-1690); 25 finale-walls-close (1693, finale.active && half decreased over 10 s); 26 render-under-budget (1699, 0 < lastRender.drawCalls < 700).

=== 4) tests/smoke.mjs — boot + budgets ===
Boots `?test=1`, 60 s ready wait. Contract check FIRST (33-39): REQUIRED = ['start','step','state','teleport'] must be functions or hard error naming the contract. Logs __FETCH.version. Teleports through ACTS = ['bedroom','house','basement','graveyard','forest','clearing','cave','mirror'] (10). TRAPS: before 'cave' it injects `if (!flags.has('waterfallTaken')) director.waterfallTaken(); skull.vanish()` (50-54, "preserve the story's one broken promise"); after 'mirror' teleport it zeroes yaw/pitch (56-61); mirror steps 540 frames vs 90 for other acts (62); MUST call `__game.render()` before sampling state (63-66: sim-only stepping doesn't render — reading state first used to leave the previous act's counters in place while claiming every act was under budget). Per-act assertions: state.act matches; no new page errors; render counters exposed; drawCalls < 700; geometries < 1500 (69-81). Screenshots via canvas toDataURL (page.screenshot can't composite the accelerated canvas headless; preserveDrawingBuffer on — 83-88) → tests/shots/<act>.png. Then: render-free simulation burst 300 steps, simFps > 25 (92-99); worst-act summary re-asserts drawCalls<700 / geometries<1500 + shape {drawCalls,triangles,geometries,textures} (103-124); final zero page/console errors (126). Report → tests/results/smoke.json. Budgets are GLOBAL per act (700 draws / 1500 geometries), not per-act-differentiated.

=== 5) Debug contract (src/main.js) ===
window.__FETCH (main.js:1487-1563): version ('0.5.0-intruder', :24), feelProfile (FEEL_PROFILE), ready:true, start(), pause(reason='debug'), resume(), restartCheckpoint(), step(dt=1/120, n=1, render=true) — uses g.input.frame(i===0); stepWith(seconds, controls={}, render=true) — n = max(1, round(seconds/FIXED_DT)) with FIXED_DT = 1/120 (:19); control frame fields: moveX, moveZ, run, lookX, lookY, throwHeld, callHeld, throwPressed, throwReleased, callTap, interactPressed, jumpPressed; TRAP: after the first fixed step all edge fields (throwPressed/throwReleased/callTap/interactPressed/jumpPressed) are force-cleared (:1511) — an edge lasts exactly one 1/120 step; state() → {act, paused, pauseReason, pos, yaw, pitch, skull:getState(), enemies:[{kind,state,pos}], flags:[...g.flags], finale:{active,phase,half}, render:{drawCalls,triangles,geometries,textures}}; teleport(act) (auto-starts game); setSkull(x,y,z,vx,vy,vz,mode) — mode ∈ ['held','outbound','returning','anchored','gone'], reparents out of hold, seeds flightTime/outboundDuration=1/hardAway=2/maxRange=60 (:1536-1555); setStage(n); async shot(name) POSTs canvas PNG to server endpoint '/__save'.
window.__game = game (:1709) — the white-box companion (regressions assumption). Flags: `this.flags = new Set()` (:131), `flag(name){ this.flags.add(name) }` (:846); namespaced marrow flags use 'marrow:*'. spawnLog: lazily created array of [kind,state,x,z,time] (enemies.js:984,1052), trimmed/deleted on restore (main.js:482-483,562-563).
TEST_MODE = ?test or ?autotest (:21). TRAP: in TEST_MODE with !this._selfStep the rAF loop ONLY renders — it never steps the sim (:1457-1461); all simulation must come through F.step/F.stepWith. hitStop freezes sim but drifts cosmetics (:1462-1471). Teleport contract: teleport() ALWAYS calls _completeArrivalInstant() first (:1103-1124) — jumping to ANY act before the bedroom arrival sets 'skullArrived', completes bedroomArrival, restores+holds the skull, settles any mid-flicker; idempotent. teleport also kills fade transitions in TEST_MODE (:1125-1128), 'mirror' branches to finale.begin() (:1129-1132), teleporting AWAY from forest resets forest._lastIdx/sealS=-10/entered=false, INTO forest recentres, clearing/cave seat _lastIdx at length-1 (:1144-1152), and abortSwing() so you never wake holding a rope (:1141). Two-pass rendering (world layer 0, held layer 2 with cleared depth) is why lastRender.drawCalls sums both passes (:1393-1417) and why the lantern/hold layer-mask regressions exist.

=== 6) tools/ inventory (one line each) ===
- audit-ceiling.mjs — geometry forensics: small rendered meshes crossing an authored room ceiling plane.
- audit-furnishings.mjs — static (no browser) furniture-vs-openings audit of house.js furnish() K.* placements against HOUSE_TABLES doors/windows/stairs/shafts.
- package-netlify.mjs — build the flat standalone Netlify-drop ZIP (dependency-free, reproducible, forward-slash entries; --output=).
- verify-netlify-release.mjs — clean-extract the ZIP to temp, validate entry paths, serve only that dir, real-GPU boot to ready (does not trust the working tree).
- probe-basement-fixes.mjs — shots of the two basement repairs (cellar-doorway planks, furnace fire).
- probe-bounds.mjs — walk out of every act in 12 compass directions; report escape distance/falls.
- probe-bridge.mjs — raise the clearing bridge; report every stone's risen top vs ground ("one more stone here" gap as a number).
- probe-chain.mjs — ride THE CHAIN through the real input path; report each link's latch, arc, landing.
- probe-exterior.mjs — corrected-FOV real-GPU composition plates for graveyard + forest (canvas readbacks, never page.screenshot).
- probe-finale.mjs — deterministic finale probe: room ownership, live look, contact, hard black, WebGL frames.
- probe-finale-intensity.mjs — truthful finale plates preserving the waterfall's broken promise (skull gone, empty hands), cosmetic shake/FOV cleared.
- probe-forest-spline.mjs — measure corridor width/height/heading along the spline for chain-anchor placement.
- probe-grave-arena.mjs — real-GPU visual/perf/audio/combat probe of the graveyard pass (rise/pressure/strike/stun/pop captures + JSON).
- probe-graveyard.mjs — deterministic arena fight via real throw grammar; reports any body that fails to join or finish.
- probe-hitch.mjs — rAF frame-gap vs renderer.info.programs.length to find area-entry/skull-hit stalls (no pixel reads).
- probe-programs.mjs — name the shader programs that compile during play (diff renderer.info.programs across act changes/hits).
- probe-horror-expansion.mjs — real-GPU visual/perf/audio probe for the under-falls horror lane (5 captures + JSON).
- probe-house-expansion.mjs — frozen visual plates of house/basement expansion states (mechanics live in tests/house-expansion.mjs); viewmodel layer 2 omitted from capture.
- probe-lantern-curve.mjs — the carried lantern's falloff as a picture (58 cd / decay 1.6; albedo clipping math in header).
- probe-light-attribution.mjs — turn each light off in turn to name which one bleaches a prop (includes no-decay directionals).
- probe-light-budget.mjs — every light ranked by irradiance at 1 m vs the ~20 bleach threshold for this kit's 0.03-0.06 albedos.
- probe-ossuary.mjs — whole-district proof: arrival, kennel, niches, counterweight (real throw/anchor/hold), input climb, E-verb exit, forest mouth.
- probe-prop-surface.mjs — is the texture bound at all (uv+map) and what the painted canvas averages in linear albedo.
- probe-ravine.mjs — fall into the ravine on purpose: fall catch, death, respawn placement.
- probe-skull-presentation.mjs — real-GPU proof of throw tether + ordinary catch settle.
- probe-statue-hit.mjs — one-off: why a thrown skull didn't stagger a hunting mourner.
- probe-stuck.mjs — walk the whole forest corridor N times; report every stall with the corridor systems' beliefs at that moment.
- probe-swing-arc.mjs — does the ravine rope carry? asin(GRAV/PULL)=asin(14/30)=27.8 deg lift threshold; records the whole arc.
- probe-underfalls.mjs — waterfall/underfalls vistas with per-vista draw calls so a pretty cave can't blow the shared budget.
- probe-untextured-props.mjs — every no-map material ranked by world volume wearing it (the "plastic" list).
- probe-viewmodel-light.mjs — cradle/skull/world luminance ratio per act, held and thrown (rule: cradle ≈ 1.5-3x world).
- probe-warm-experiment.mjs — decide shader warmup strategy by measurement (game must be STARTED first; the unstarted tour was an invalid instrument).
- render-sculpt.mjs — capture the standard skull-off shot set for one sculpt module via sculpt-harness.html.
- sculpt-harness.html — standalone page rendering any skull sculpt module under the game's real bone material/lighting (?mod=, drive via window.__H); not part of the game.
- shot-areas.mjs — every act from spawn: forward, +120, +240, straight up.
- shot-aug14.mjs — visual pass for the Aug 14 feedback batch (pit mouth, marrow rope, hunting statues, floating key, ossuary lid handle, cellar skirt, pool murk).
- shot-enemy.mjs — deterministic enemy plates in their real encounter lighting (hands/skull meshes hidden, skull light live, fear FX neutral).
- shot-forest.mjs — walk in and capture the corridor from inside + a look back at the seal.
- shot-hands.mjs — the EMPTY hands ("these do not look like human hands"), the unflattering no-skull case.
- shot-held.mjs — held viewmodel composition for a given ?skull= variant.
- shot-horizons.mjs — look OUTWARD from every place Alex named end-of-world sightlines.
- shot-lid.mjs — ossuary lid + handle from the climb platform.
- shot-props.mjs — close-ups of individual objects (room distance flatters boxes).
- shot-room.mjs — place the player anywhere (act x y z yaw pitch) and capture; yaw convention documented in header (forward = (-sin yaw, -cos yaw)).
- shot-visual-audit.mjs — one pass, every act, player-height vantages, numbered PNGs + per-frame draw-call report.

NOTES / DISCREPANCIES: (a) playthrough has 49 `beat(` calls, not the remembered 48 — beat 'entered-the-forest' (line 658) plus 'path-sealed-behind' (696) are separate. (b) The 97 regression count is 82 content checks + 15 per-scenario 'zero browser errors' checks (one content check, clearKeys, can degrade to SKIP which still counts, reported passed). (c) Out of scope but present in tests/: 13 further suites (house-critical-path-regression.mjs 1084 lines, house-expansion, house-chase-doors, house-return-horror, grave-arena, forest-nervous-system, forest-hardening, choir-route-occlusion, district-culling, enemy-stain, enemy-standing-postclear, failure-state, pause-title, perf-pool, pump-release-recovery, creature-audio, basement-foundations, exterior/horror/underfalls-expansion, netlify-release-integrity, render-perf) — a route restructure would also hit those; they use the same harness. (d) smoke's budget is uniform per act (drawCalls<700, geometries<1500) — no per-act differentiated ceilings exist despite the "per act" phrasing; the worst-act summary re-asserts the same numbers.


---

# MAP: SKULLPOWER

REPORT — FETCH skull power + enemies (repo C:/Users/Alex/Projects/fetch-aug15)

=== 1) game.skullPower ===
WRITE (only one): src/outside.js:554 `game.skullPower = 2;` inside the idempotent applier in buildSealedMausoleumSecret's ticker (outside.js:552-577), gated `game.flags.has('skullSharpened') && !game._sharpenedApplied && game.skull?.sockets`. Reapplies after respawn/reload because it keys off the persistent flag, once per Game instance via `game._sharpenedApplied`.
READS (all of them):
- src/enemies.js:1884 `const power = game.skullPower || 1;` inside Enemies.update skull-contact block. Effects:
  - enemies.js:1885 speed gate: contact counts only when `skull.vel.length() > 8 / power` (8 m/s normally, 4 with canine) and skull.mode is 'outbound'|'returning'.
  - enemies.js:1905 stun duration `e.stunT = e.spec.stun * power` (x2).
  - enemies.js:1909 knockback `e.knockV = travelDir * clamp(speed * 0.03 * power, 0.45, 0.9 * power)` (both slope and cap x2).
  - enemies.js:1913 hit-marker flourish `skull._flourishT = 0.3 + (power - 1) * 0.2`.
- src/marrow.js:766 hunting Mourning Statue shove `const push = 1.9 * (game.skullPower || 1)` (marrow.js:759-772).
- Comments only: outside.js:316, enemies.js:1880-1883 (explicit law: power NEVER bypasses the stun-then-deliberate-second-throw grammar), docs/HANDOFF.md:487,704-708.
GRANT CHAIN (buildSealedMausoleumSecret, outside.js:319-579): destructible hero grave #6 (`game.destructibleGraves[5]`, keyGrave() outside.js:435) toppled at hits>=2 (outside.js:511) reveals floating key at (18.52, 0.85, 37.5) (outside.js:439). Key fetch target 'mausoleumKey' (outside.js:454-468) is OUTBOUND-ONLY (`if (skull.mode !== 'outbound') return 'continue'`, :457) plus a 0.9 s settle window (`keyRevealedAt`, :453,520-522) — Alex accidentally scooped it on a return leg once; this is the fix. Lock target 'mausoleumLock' (outside.js:471-488): outbound-only, requires `skull.carry.id === 'mausoleumKey'`, consumes via dropCarry, flags `mausoleumUnlocked`; wrong/no key gives impact('locked') + lockedRattle. Grate sinks by derived collider (outside.js:545-548). Canine target 'ironCanine' (outside.js:490-505): outbound-only, requires mausoleumUnlocked && !skullSharpened, flags `skullSharpened`, impact('pop'), `skull._flourishT = 0.9`.
VISUAL IDENTITY OF POWERED SKULL (outside.js:552-576): sockets deepen `socket.scale.multiplyScalar(1.13)` (:555, skull.sockets comes from variant parts, skull.js:143); inverted-hull rim: MeshBasicMaterial 0x9fb4b8, THREE.BackSide, transparent opacity 0.4, depthWrite false, toneMapped false (:561-564), a shell clone per opaque skull mesh at scale x1.06 with copied layers.mask (:566-576). Value-only, per Alex's colorblind law. Shells are children of skull.root submeshes, so holdNow()'s LAYER_HELD traverse (skull.js:664) carries them.

=== 2) Skull flight model ===
FEEL_PROFILE (FROZEN): src/skull.js:17-48, `Object.freeze`, name 'fetch-core'. Shape: launchBase 26, inheritVel 0.22, outboundBase 0.8, hardAwayBase 1.9, poiseDrift 6.5, poiseMax 22, bounceReturnTime 0.40, guideStrength 8.5, gravityOut 6.5, maxRangeBase 14 + maxRangeCharge 26, bounceGain 1.05, bounceCap 44, returnFallback 2.4, returnStuckFallback 0.7, returnSpeedFloor 26, returnSpeedRetention 0.93, returnSpeedCap 50, returnSnapBonus 14, returnBendRate 9, returnSnapBendRate 15, catchRadius 1.35, cradleDist 2.4, cradleDrop 0.75, snapBuffer 0.12. chargeTime/launchCharge marked unused ("the button is the tether").
MODES: skull.js:50 `['held','outbound','poised','returning','anchored','gone']`. Transitions: tryThrow (:701-736, held→outbound, press=throw / HOLD=stays out / release=return); call() (:738-750, duration-only quick call, snap return, kick-ball law); beginReturn(reason) (:752-759, guarded against returning/held/gone, snapReturn = reason==='snap'); anchorAt(point,{swing,maxHold,puzzleId}) (:761-767); vanish() (:769-791, waterfall, light-census parking); bootAbsent/arriveRestore (:793-852, new opening); _updateFlight (:1050-1157) with held-poise tread-air branch (:1071-1086) and auto-return failsafes (:1108-1115); _completeCatch (:1190-1209). Monotonic clocks flightTime/freeFlightTime (:89-90, 1058-1062). Collision `_collide` (:1237-1279): AABB shallowest-axis pushout, reflect, bounce GAINS speed (bounceGain/bounceCap), `c.skullPass` colliders are transparent to the skull (:1255; the marrow altar collider is skullPass, marrow.js:411-412). Pinball guard `_bounceFx` (:1281-1303): 3 bounces in 0.4 s → auto return (or force returnStuck past fallback when already returning).
HIT RESOLUTION — TWO SEPARATE SYSTEMS:
(a) fetchTargets: skull.js:_checkTargets (:1305-1323) sweeps prevPos→pos against world.fetchTargets spheres; onHit returns directive 'return' (beginReturn('hit')), 'gone' (vanish), 'continue' (no interruption — pierce grammar ALREADY exists here), or anchor-via-anchorAt. Runs in outbound, poised, and returning updates. world.addFetchTarget: src/world.js:315-319.
(b) ORDINARY ENEMIES ARE NOT fetchTargets. They are resolved inside Enemies.update, src/enemies.js:1875-1926 ("---- skull contact ----"). Per-enemy `e.iframes` decays (:1879), hit granted 0.6 s iframes (:1888). Gate at :1885: `e.state !== 'dying' && e.iframes <= 0 && (skull.mode === 'outbound' || skull.mode === 'returning') && skull.vel.length() > 8 / power`. Geometry `_skullIntersects` (:3052-3069): swept segment (skull.prevPos→skull.pos, skullR 0.11) vs 3 volumes from e.spec.hit — vertical body capsule (y0..y1, r), shoulder sphere (shoulderY, shoulderR), head sphere (headY, headR); helpers pointSegmentDistanceSq (:422-433) and segmentVerticalDistanceSq (:438-489).
THE STUN-THEN-POP GRAMMAR (three branches, all end with velocity deflection along `away` + beginReturn('hit'), and none touches a flight clock — comment :1890-1892 "the carom ... No clock is touched, so the kick-ball law survives intact"):
  1. e.state==='stunned' && e.spec.hp !== Infinity → `_pop` (:1896-1900): _pop() at :2401-2424 (LOUD tier, gore, stain, wakeAll 30 m, director.onPop); then `skull.vel.copy(away)*speed*0.5`, jaw grin 0.65, beginReturn('hit').
  2. e.state !== 'stunned' → QUIET stun tier (:1901-1916): stunT = spec.stun*power, recoilT 0.12, knockV, impact('hurt'), loop.choke(), flourish CLACK, `skull.vel = away*speed*0.35`, beginReturn('hit').
  3. stunned unkillable re-hit (:1917-1924): impact('locked') collapsing ring, dull thud, `skull.vel = away*speed*0.45`, beginReturn('hit').
WHERE PIERCE HOOKS WITHOUT TOUCHING FEEL_PROFILE: the three `skull.beginReturn('hit')` calls at enemies.js:1900, :1916, :1923 (plus the two/three `skull.vel.copy(away).multiplyScalar(...)` deflections at :1898, :1915, :1922). Not returning (and not deflecting, or deflecting less) leaves the skull in 'outbound'/'returning' with momentum; the per-enemy 0.6 s iframes (:1888) already make multi-enemy contact along one sweep well-formed, and the enemy loop iterates every enemy each frame, so a continuing skull naturally hits the next body on later frames. FEEL_PROFILE and skull.js flight code need zero changes; the swept segment is skull.prevPos→skull.pos which skull.js maintains in every mode.
SPECIALS:
- Kneeler & Resident: same enemies.js path but `hp: Infinity` (KIND, :14, :19) → can never take branch 1; only quiet stun (Kneeler stun 0.4 s, Resident 1.6 s) and the 'locked' ring.
- Drowned Choir: entirely excluded — `if (e.kind === 'choir') { ...continue; }` at :1275-1282 runs before the skull-contact block; resonancePulse also skips choir (:1200 "there is no skull here in story"). The skull cannot touch it at all.
- Mourning Statues (marrow.js): NOT in enemies.list; their own proximity test in the marrow ticker, :731-733 — `sd < 1.0` XZ + y in (FLOOR, FLOOR+3), modes outbound|returning, per-statue hitCd 0.7 s. Truce branch (:735-749): shiver + crack, position untouched. Hunting branch (relicKept, :759-772): stagger 1.5, shove 1.9*skullPower clamped to hall bounds, impact('pop'). Neither branch deflects or returns the skull.
- The Presence (marrow.js:867-874): `sd < 0.55` → flinch only (forced twitch + whisper, 0.8 s cooldown). The skull passes THROUGH — no beginReturn, no vel change ("your verb is not from its game").
TRAP: skull.js V.* vs W.* scratch-vector aliasing warning (:57-63) — _checkTargets must use W.* only.

=== 3) Enemy taxonomy ===
KIND table src/enemies.js:7-23 (h, r, chase, stalk, hp, stun, scale, windup, strike, strikeRadius, recovery, lunge, hit volumes):
- walker: h 2.05 r 0.3 chase 5.6 stalk 1.1 hp 1 stun 2.8, windup 1.1 strike 0.66 strikeRadius 0.96 recovery 0.42 lunge 0.72. ORDINARY (poppable).
- resident: h 2.5 r 0.38 chase 4.9 stalk 0.9 hp Infinity stun 1.6 scale 1.22. SPECIAL: unkillable house hunter; opens doors (_tryOpenDoor :2664-2695, knob-rattle tell 0.55 s, opens at 1.15 s); YIELDS while skull anchored in a puzzle (`game.skull.anchor?.puzzleId`, :1297-1314 — backs off inside 4.4 m, threat floor 0.55); director spawns it via residentHeard (director.js:573-586, at (-3,-12) 'stalk') after HOUSE_RESIDENT_DELAY 18 s (director.js:27, _updateResident :588-606); loses interest at chase windT>9.
- kneeler: h 4.4 r 0.9 chase 6.2 stalk 0 hp Infinity stun 0.4 scale 2.4, windup 2.2. SPECIAL forest set-piece: placed dormant at ~93% of path (director.js:895-903, `mesh.rotation.x = 0.5` "it kneels. do not wake it."); wakes at d<16 && player.noise>0.6 (director.js:918-921).
- choir (Drowned Choir): DROWNED_CHOIR spec enemies.js:29-39 (h 2.75 r 0.42 warning 2.65 drySpeed 1.55 heardSpeed 4.35 attackRange 1.85 attackCommit 1.05 attackRadius 1.18 recovery 1.25). SPECIAL cave actor, skull-immune, sound-hunter: states warning/stalk/pressure/recover/recoil/spent (_updateDrownedChoir :2117-2399); follows e.heardPos (last sound), never live player pos; caveSpray (:1092-1124) wets/washes it; first caught pressure wave is a non-lethal teaching hit via e.warned (:2194-2210); second kills via director.death. Singleton `this.choir`; API beginDrownedChoir/endDrownedChoir/drownedChoirHear/drownedChoirDrench/getDrownedChoirState (:1008-1151).
MARROW ACTORS (not Enemies instances): Mourning Statues (marrow.js:359-384, weeping-angel movement :711-794 — turn only unseen in truce, DASH 3.3 m/s unseen when relicKept, touch d<0.85 = death :793); The Presence (buildPresence :70-224, guard/erupt/fold anims :529-899, yields at dist<1.15 :888); Dark Eyes (:422-441, 797-819, ephemeral scare).
WALKER SUBTYPES BY FLAG (all spec 'walker'): e.standing (the Standing Kind — moves only unobserved, _isObserved :2592-2612, home leash 24 m :1345, optional e.tether :1353); e.graveArena / e.gravePressure (graveyard wave/pressure bookkeeping, claim-token system :1238-1269 budget 1→2 at wave>=2); e.forestChaser / e.forestCompany / e.forestArena (director.js:789-814, 1145-1162, 835-882).
STATE MACHINE (ordinary): 'dormant' → 'wind' (mercy wind-up, arms tell, :1425-1438) → 'chase' (house BFS nav _houseRoute :2454-2553, besiege :2619-2644, unstick ladder incl. unseen-only relocate :1618-1664) → 'strike' (fixed-point commitment, strikePos copied once, _beginCommittedStrike :1962-1977, only one striker globally + oldest-serial wins :1703-1710, landed → game.director.death(e) :1744) → 'recover' → back. 'stunned' (:1767-1791: convulsion, material dim via color.setScalar — hue-free, resumes 'wind' at windup*0.5) and 'dying' (:1793-1816: pop launch tumble-shrink, or quietRest sink from _layToRest :2426-2441).
GROUND-EMERGING (the rise): spawn() enemies.js:963-1002 — EVERY non-dormant/non-standing spawn gets `e.graveRiseDur = 1.15; e.graveRiseT = e.graveRiseDur` (:994-1000) + walkerRise sfx outside graveyard. Render of the rise at :1840-1858: squash `mesh.scale.y = spec.scale * (0.34 + 0.66*up)`, sink `mesh.position.y -= remaining^2 * 1.35`, lateral shudder, jaw hangs open; `e.riseFrozen` lets an author own the clock (dormant guard :1322); `e.riseSquashOnly` grows from a crouch without sinking through the storey below (nursery). Nursery music-box walker: director.js:538-557 — graveRiseDur 11.5, riseFrozen, riseSquashOnly, rise driven by box wound state, rewind pushes it back down (:531-537). Graveyard arena waves: director.js:673-703 — spawn 'wind' with graveRiseDur 1.08 and windT = -i*0.13 stagger.
SPAWN/PLACEMENT API: Enemies.spawn(kind, x, z, state='stalk', yHint=3) (:963; yHint picks the STOREY via groundHeightAt); beginDrownedChoir({pos,heardPos}) (:1008); wakeAll(x,z,radius) (:1180); clear(pred) (:1153); resonancePulse(pos, radius=8, stun=1.5) (:1197-1222 — carved-grave crowd control; in graveyard a pulse on an already-stunned finite-hp arena body lays it to rest quietly :1206-1211); resetGraveClaims (:1957); resetStains/stainState (:926-942, STAIN_CAP 48 ring). Spawns log to game.spawnLog (:984).

=== 4) Danger-sense relic ===
Everything lives in src/marrow.js (buildMarrowArea, called from outside.js:306).
STATE: the `state` object assigned to `game.marrow` (marrow.js:462-475): inMarrow, witnessed, yielded, relicKept, escorted, dangle (set :948), presence, statues, guardianDist, plus flags 'marrow:entered/witnessed/guardianYielded/relicKept/escorted' (:564,571,890,940,957). Module-closure state survives death (statues reset via _deadSeen :692-702; the dangle persists because it is parented to skull.jaw).
TAKE: relic fetch window is inline in the ticker, NOT a fetchTarget (:935-996): outbound|returning proximity rd<0.72 & |dy|<0.9; take requires `state.yielded && skull.mode === 'outbound'` (:938); on take, relicMat snapped to resting emissive 0.8 before clone (:943), dangle = relicGeo.clone()/relicMat.clone(), scale 0.45, position (-0.048,-0.035,0.05), `game.skull.jaw.add(dangle)` (:944-948) — jaw-mounted, permanent, NOT skull.carry. Refusal branch (:982-994): dead knock + relic flinch; pre-yield it also points at the guardian (twitch + eye-flare + whisper).
HEARTBEAT PULSE (:664-679), the actual "danger sense": gated ONLY on `if (state.dangle)`, and it sits BEFORE the `if (!state.inMarrow) return;` at :681 — so it runs every frame in every act once the relic rides. Implementation: near = min over ALL game.enemies.list of hypot XZ distance (any kind, incl. choir; dormant included); `danger = clamp(1 - near / 14, 0, 1)`; `state._pulseT += dt * (0.9 + danger * 5.1)` (0.9→6.0 beats/s); `beat = max(0, sin(_pulseT * TAU)); throb = beat*beat`; scale = 0.45 + throb*(0.1 + danger*0.16); emissiveIntensity = base (captured via `m.userData.base ??=`) + throb*(0.4 + danger*2.4). Value/motion only.
GRANTING DANGER SENSE WITHOUT THE MARROW RELIC: there is no dangerSense flag or standalone system — the pulse driver is welded to `state.dangle` (a jaw-mounted emissive mesh) inside the marrow ticker. Granting it elsewhere (e.g. combined with the canine at the hero gravestone, per the notes) means either (a) setting game.marrow.state.dangle to some other jaw-mounted emissive mesh (the driver only needs .scale + .material.emissiveIntensity), or (b) lifting :664-679 into a neutral home. TRAPS: game.marrow exists only after buildGraveyard→buildMarrowArea runs; the driver reads game.enemies.list directly (marrow statues and Presence are NOT in that list, which is why the crypt line at :663-664 says "in the crypt, in the yard, everywhere it rides" — in the crypt it actually only beats for list-enemies, none of which exist there); distance is horizontal-only; nothing distinguishes threat states (a dormant statue-walker counts). Note also the parallel EXISTING radar: skull chatter threat (skull.setThreat fed each frame by enemies.js:1952, expressed as jaw chatter rate skull.js:992-996 and eye tracking) — danger sense is a second, visual channel on top of it.

=== 5) Jaw/carry system ===
MOUNT: jawMount (variant part, skull.js:141/208-210) rides the jaw; carryLight PointLight 0xd6b879 intensity 0 distance 1.15 decay 2.0 on layer 0, parented to jawMount (skull.js:126-128 — "a tiny jaw lamp only exists to make a carried key or charm readable in flight... never changes progression").
grab(id, mesh) skull.js:673-690: `this.carry = { id, mesh, scale: mesh.scale.x }`; mesh reparented to jawMount at (0,-0.018,0.057) rot (0.4,0,0); scale clamped `min(scale.x, 1.15)` ("do not make the objective vanish at the exact moment it is acquired"); carryLight 0.82; jaw 0.3; flourish 0.45; catchThud CLACK rate 1.6 + unlock chime 1.3.
dropCarry() skull.js:692-699: removes mesh from jawMount, returns {id, mesh, scale}, carryLight 0.
Held-jaw law: an occupied mouth never seals the objective — `jaw.rotation.x = Math.max(jaw.rotation.x, 0.14 + clampTick)` (skull.js:1007-1011); flight jaw opening carry ? 0.3 : 0.55 (:733). Layers: holdNow puts all non-light root descendants (incl. carried mesh) on LAYER_HELD (:664), tryThrow back to 0 (:711); carryLight stays world layer 0 always (:661-663 comment).
CARRIERS: bedroomKey (house.js:1543-1552, id 'treeKey' — NO outbound guard); stairKey (house.js:2152-2160 — NO outbound guard); hatchKey (house.js:2885-2893, enabled false until armed — NO outbound guard); mausoleumKey (outside.js:454-468 — OUTBOUND guard + 0.9 s settle; the guard exists because the reveal-and-scoop-on-return bug actually shipped, comment :448-453). Consumers: mausoleumLock outside.js:471-488 (outbound-only); hatch keyhole house.js:3041-3047; carried-key doors via main.js:1276-1283 (interact on a locked door whose `d.locked` id matches skull.carry.id) and wireCarriedKeyDoor helper (house.js:1468-1471 consume path, registered at house.js:2163-2165 for stairDoor).
DERIVED-VISIBILITY LAW (the canonical instance, outside.js:508-522): per-tick, never event-set — `const carried = game.skull?.carry?.id === 'mausoleumKey'; key.visible = carried || (toppled && !gotten && surfaceVisible)` where surfaceVisible defers to district seals (`!game.ossuary?.inOssuary && !game.marrow?.inMarrow`); comment: "a CARRIED key is always visible: this line used to hide the mesh the moment it was grabbed, which is the whole of 'the skull wasn't holding a key'". keyTarget.enabled likewise derived (:522). Same doctrine: marrow relic `relic.visible = !state.relicKept` (marrow.js:904); key/glow breathing keyed off visibility (:524-530). Also derived: mausoleum grate collider follows the sinking mesh, never a flag (outside.js:547-548).
PERMANENT JAW RIDERS (not carry): the locket keepsake — outbound-only + gotBedroomKey-gated fetch (house.js:1589-1602+, "it clamps the chain in its teeth and never lets go"); the marrow relic dangle (marrow.js:944-948). Both are direct jaw.add children, immune to dropCarry.
ANCHOR/PUZZLE CONTEXT (feeds Resident yield): anchorAt opts carry puzzleId — kennel outside.js:1663, ossuaryCounterweight outside.js:1862, house puzzles house.js:3326, 3923 (window relay), 6057, archiveDraft house.js:5979.

TRAPS / GOTCHAS SUMMARY: (1) enemies are not fetchTargets — a pierce must be implemented in enemies.js:1896-1924, not in skull._checkTargets; (2) never touch flight clocks or FEEL_PROFILE in a hit branch (comment enemies.js:1890-1892; kick-ball law); (3) 8/power speed gate means power also widens WHAT counts as a hit, not just its effect; (4) Choir and Presence must stay skull-immune (story law, enemies.js:1200, marrow.js:867); (5) the canine applier is idempotent-by-flag — any new combined upgrade must follow the same reapply-on-respawn pattern (outside.js:551-557); (6) three of four key grabs lack the outbound guard the mausoleum key needed; (7) marrow dangle pulse runs globally but only sees game.enemies.list; (8) V/W scratch-vector aliasing in skull.js (:57-63); (9) skull.vanish/bootAbsent must park lights in world.lightRoot or the shader light census recompiles every material (skull.js:772-787).
